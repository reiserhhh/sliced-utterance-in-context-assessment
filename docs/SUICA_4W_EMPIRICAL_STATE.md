# The 4W theory: empirical state after the T, U, and W lines

Written 2026-08-19 by the planner, closing the arc the owner opened with
`docs/SUICA_4W_PERSON_INTERPRETER_THEORY.md` and Codex's M4-T1. This
document maps every measured result back onto the theory's own sections —
the feedback the theory's §11 interface asked for. Tier for everything
below: EXPLORATORY, corpus-level, Reddit cohorts (PANDORA Big5 cohort
N=1401-derived pools; the disjoint cohort N=8,895-derived pools), author
structure only; no person claims; no psychological naming of technical
objects (§5.4 binds throughout, and the data now vindicate it).

## §2.3 Where — measured state (T-line, closed 2026-08-17)

- Selection deviations decompose into 4–5 reproducible nested innovation
  levels plus a tail no tree exhausts (T1, adopted; transported by W2:
  flat reproduces, the tail carries MORE on fresh authors — residual
  0.9722 vs 0.9552, depth 6 newly resolved).
- The tail TRANSPORTS to never-visited communities (T2: early taste
  predicts unseen-community entry, AUC 0.6031) — the tail is a
  generalizing coordinate, not memorized residue.
- The identity budget (T3): the low-rank taste coordinate carries 82% of
  identity bits; the tree is subsumed by its own leaves; observability
  moderate.
- The carrier is METRIC-BORNE (SR3/SR4, capacity exhausted by rotation
  proof 5.3e-15): what separates identity-optimal from trait-coupled
  representations is PPMI-vs-Hellinger geometry — the common-community
  mass Hellinger keeps and PPMI discounts.
- Proposition: **identity lives in the distinctive; personality lives in
  the common** — with its When clause below.

## §2.4 When — measured state (U-line, closed 2026-08-18)

- The first positive projection of the transition kernel K_u (eq 12):
  event order carries reproducible author structure (U1, rho 0.2893;
  transported in cell by W2 at 0.2207). The channel is DWELL — the
  theory's crossing-#4 candidate "inertia," measured label-free: one
  stay-rate scalar carries 62% (source) and reproduces almost exactly
  across cohorts (0.1803 → 0.1778). Order is reproducible but REDUNDANT
  with the bag for absolute identity at coarse state resolution.
- Slow time: the person is a MOVING distribution (U2, DRIFT_WITH_CORE) —
  the personal excess loses ~46% over three years against an
  epoch-matched baseline; only a THREE-YEAR core is demonstrated (no
  flattening at the corpus horizon). All decay LEVELS are cohort-owned
  (the disjoint cohort is more persistent across the board).
- The eq-12 projection caution held its ground both ways: positive
  projections were claimed as projections, null ones never as absence.

## The crossings (§3) — measured state

1. **Where × Who (selection identity):** established, budgeted,
   metric-typed, and transported (T-line + W2). Identity ≠ psychology at
   every step (T1's boundary, kept).
2. **Where × What (condition response):** the V8/M-N-P-Q machinery's
   territory (gauge = frame-agreement instrument; tax curve κ(V) as
   state-channel response) — prior to this arc, unchanged by it.
3. **What × When (state trajectory):** still the least-posed crossing;
   the U-line measured selection-side dynamics only. OPEN.
4. **Who × When (individual dynamics):** inertia/tightness/drift EXIST as
   reliable author structure (split-half 0.77–0.94) and are **Big5-silent
   at corpus power** (U3: nine of nine estimates in-band while the bag
   channel reproduces SR1's r = 0.049 on identical pairs). The theory's
   demanded opportunity-structure control (cross-thread) was applied
   BEFORE the label join. The dissociation family's fifth confirmation.

## The layer structure in time (U2b/U2c/W1 — the middle, closed)

Two regimes, supported on both cohorts: the SLOW end is the common
allocation and the taste direction (statistically tied; strict ordering
retired); the FAST end is the distinctive tail (Λ = +0.0911
[+0.0628, +0.1490] on 3,241 fresh authors — COMMON_STANDING). The When
clause of the T-line proposition:

> **The distinctive is the ink — it fades fastest. What stands is the
> common scaffold and the taste direction written through it.**

Re-identification survives motion not because people stand still but
because others are far away (U2: AUC 0.9837 coexists with a 53% three-year
floor share over a 0.05 cross floor).

## §6 noise (target-relative) — vindicated

The dissociations ARE the theory's noise table in action: the same
component is signal for one target and silence for another —
identity's dominant carrier is trait-silent (SR2); When coordinates are
identity-bearing and Big5-silent (U3); the typology communities are
criterion-adjacent for trait coupling (S-line −41%) and nearly irrelevant
for identity (−7.8%). "Noise is target-relative" is now a measured fact
of this corpus family, not a stance.

## The transport chapter (W-line — new; the theory did not yet have one)

**Structure and ratio laws transport; levels belong to the cohort.**
Five instances: Λ's sign closes while λ levels halve; the floor-share
ratio reproduces while D shifts; the rho cell and stay scalar reproduce
while the raw level shifts; flat reproduces while path/residual
redistribute; the LAW arm (re-instantiated rules) transports fully while
the frozen INSTRUMENT transports at reduced effect and cannot carry its
own mass split (0.347 vs 0.50). Corollary for all future cross-cohort
work: compare size-matched, transport rules not artifacts, and never
register single-factor level leans across cohorts.

## Standing methodological laws purchased by this arc

- **Discrimination is not stability** (AUC over-predicts geometric
  persistence; stability may never be inferred from a discrimination
  score).
- **Reproducible is not additive** (an order channel can be real and
  redundant).
- **A precision gain is not a contradiction** (the RESOLVES transport
  class).
- Defects #68–#83 with their conventions (registry notes twentieth
  through thirtieth) — operating points for synthetic worlds; census
  computations, gate predicates, and ID-gates pre-executed; width
  projections with per-unit noise; ratio-free contrasts; transform
  domains under nulls; registration by quoted formula; linear-partial
  assumptions.

## §10–11 — the theory's own priority list, answered

1. Condition-matched residual audit — DONE (T2).
2. Tree + continuous joint representation — DONE (T3/SR3/SR4,
   METRIC_BORNE).
3. Mapping to 4W — DONE (this arc; this document).
4. Frame-refresh / dynamics falsifiers — the When quadrant measured on
   both time scales with its own falsifiers (U-line).
5. **External connection — the one remaining priority.** Everything here
   is one platform's selection behavior. The external-validity step
   (native corpus — owner-paused; or any off-platform criterion) is the
   program's next frontier and is an OWNER decision, not a loop decision.

## Open, with prices

- W3: MBTI-axis labels on the disjoint cohort — gated on its own
  governance review (new join class).
- What × When (state trajectories inside text) — the least-posed
  crossing; needs expression-side machinery joined to the selection side.
- R3 reconciliation derivation (theory-side, no data needed).
- Long-horizon floor (needs panels longer than ~4 y).
- Non-dwell order residual; finer-state order.
- JPA manuscript (#27, owner's, in progress).

## Dated addendum (2026-08-19) — the crossing table completes

With X1c and X2 (`SUICA_M4_X_EXPRESSION_RESPONSE_PLAN.md`), all four
crossings now carry real-data readings, metadata-only on the
expression side:

- **#2 Where × What (X1c):** expression volume is venue-conditioned in
  three layers — personal baseline (marginal 0.180), venue norm
  (0.093), and a real, persistent idiosyncratic response at the
  TRACE/IDIOSYNCRATIC boundary (~1.9% of variance; R = 0.279;
  reproduced on the Big5 cohort). The response operator's first
  projection is NON-EMPTY — the first crossing where the personal
  second-order channel is present rather than silent.
- **#3 What × When (X2):** expression volume has real event-time path
  structure (presence +0.111, ~58 bands), author-owned WEAKLY on the
  disjoint cohort (ρ_own 0.259) and STRONGLY on the Big5 cohort
  (0.637, 4.5× implied variance — a genuine cohort difference carried
  as X3's flag with its selection caveat); 85% intrinsic vs venue
  routing; ~46% of the owned variance is reply-chain mechanics
  (U1's lesson repeating on the What side).

The expression-side readings are VOLUME-only (no text bodies were
read); content-bearing What objects remain unopened and are a
governance decision, not a loop decision. Priority 5 (external
connection) remains the program's one unanswered theory priority.

## Dated addendum (2026-08-21) — the X-line closes; the measurement-series program lands

The X-line's nine legs (plan `SUICA_M4_X_EXPRESSION_RESPONSE_PLAN.md`;
bridge `SUICA_MEASUREMENT_SERIES_BRIDGE.md`) complete the expression
side of the crossing table and add a chapter the theory did not have —
the OWNER's measurement-series program (三枝・下司 2025):

- Crossing #2 deepened: the true mains are measured (author 0.1286,
  community 0.0552; the marginal-vs-main gap is the composition
  content); the idiosyncratic response stands at the TRACE boundary.
- Crossing #3 deepened: the ergodicity ATLAS — the conflation error is
  a property of the relation (sign-flip and 2.5× exaggeration where
  composition routes; level-consistency between behaviors); the
  three-level readout from one recording, one scale, is demonstrated
  (three-level completeness, bridge §6).
- The ownership catalogue: LEVEL parameters strongly owned (0.77–0.94),
  RESPONSE/DYNAMICS parameters weakly (0.22–0.42), Big5-cohort higher
  on every measured coordinate (the sevenfold flag, selection-caveat
  bound).
- The dissociation family's SIXTH confirmation (X3): every certified
  expression coordinate is Big5-silent while the bag channel reproduces
  SR1's 0.049 on identical pairs. The support mission's honest form:
  this system supplies three-level readouts and level-3 reliability
  instruments — not trait proxies.

Defects #85–#93; suite 1,873; the label ledger carries the X3 event.
Owner-gated frontiers unchanged: W3, external connection, native corpus.
