# The Frame Displacement Problem — Resolved to a Measured Limit

Status: **the arc closes 2026-08-03.** Twenty-one registered legs across four
lines (M4-F ×8, M4-G ×7, M4-H ×6) resolve the open problem M4-D Leg 14 opened
and M4-E2 characterized. Tier: EXPLORATORY, label-free, synthetic throughout.
The claims ledger controls.

Line syntheses: `docs/SUICA_M4_F_PANEL_DESIGN_SYNTHESIS.md`,
`docs/SUICA_M4_G_OBJECTIVE_LINE_SYNTHESIS.md`. Registrations and outcomes in
`docs/SUICA_M4_D_CURVATURE_AND_RELATION_BRIDGE_PLAN.md` and
`docs/SUICA_M4_G_OBJECTIVE_REDESIGN_PLAN.md`.

---

## The problem as it arrived

Leg 14 found the discovery objective carried a systematic frame displacement.
M4-E2 decomposed it and handed over a description that offered no handle:
**distributed** (largest single piece the S4 residual at .4015–.4464),
**world-specific in direction** at the permutation null, and **not removable by
any single term** — removing the dominant term DOUBLED the gap. The freeze
whitening's unregularized amplification was named the largest identifiable
carrier, with the caveat that the sub-40% clause was knife-edged and
order-sensitive.

## The answer

**The displacement is ~46% removable safely, and the remainder is irreducible
at this level of the machinery.**

Precisely:

| finding | evidence |
|---|---|
| It cannot be fixed with data. | M4-F, 8 legs: a finite panel can be made self-consistent and cannot be made trait-certifying. Scale infeasible (events exponent .153, 10^14x budget); composition works at fixed budget (nil → +.0234); the author axis is a validated law (gamma 1.096, x32 holdout predicted .4012 / observed .3861) — but certifies an occasion-bound, state-inclusive object, and occasion spreading does not close the gap on a causally-live, adequately-powered test. |
| It is not in the estimator's regularization. | M4-G7 proved structurally, before compute and confirmed at exactly 0.0, that the repaired ridge parameter never enters `context["v2_basis"]` — the object both displacement metrics are computed from. Leg 14's gap closes by exactly 0%. |
| It is not a units artifact of the metric. | M4-G2 disqualified the raw `offset_norm` as an optimization target (log-log slope .8796 under a pure change of units) — but M4-H1 proved by an identity, before compute and confirmed at 2.22e-16, that M4-E2's SHARES are units-invariant. Levels inherit units; ratios do not. |
| It IS in the basis's normalization, and there is a safe lever. | M4-H2 found the carrier and certified its mechanism (S3 collapses .337 → .040 in 3/3 worlds). M4-H3/H4 characterized it: safe to shrinkage ratio 1.0 (**45.79% reduction**), actively good only to ratio .2 (34.66%), unsafe at >= 4.0, undetermined at 2.0, mechanism generalizing across all three worlds at the harmless ceiling. |
| The surviving majority has no handle. | M4-H5: the repair does not manufacture a new carrier, it ENTRENCHES M4-E2's largest — S4 rises from a 40–45% plurality to a **51–52% majority** of the survivor. S4 is defined by EXCLUSION, so unlike S3 it has no code-legible construction to vary. |
| And it cannot be given one. | M4-H6: S4's own direction does NOT reproduce across 8 independent repetitions of the same world (0/3 worlds clear the repetition-shuffled null; decisive in 2, knife-edge at −.0009 in the third), while the across-world control HOLDS exactly as M4-E2's world-specificity predicts — so the miss is not a measurement artifact. **S4 is repetition-specific and not attackable by any basis construction.** |

So the open problem is converted into a measured limit: a bounded,
mechanistically-supported repair exists; beyond it, the displacement is
repetition-specific residual that no basis can capture because there is
nothing stable to capture.

## What was also built along the way

- **A certified scale repair of the estimator** (M4-G6): per-column
  standardization at alpha = .10 x deployed ridge is exactly c-invariant, ties
  the best prior recovery, and its optimum is itself scale-free at an interior
  point. The underlying defect was named and proved: `_hazard_design` mixes
  columns of exactly binary scale provenance (all 1,050 exactly degree-0 or
  degree-1 in c), so no scalar regularizer can be scale-consistent.
- **A D3 panel design specification** (M4-F): shared occasions, recruit
  authors not words — with the honest rider that what it certifies is an
  occasion-bound object, never a trait.
- **Seven standing methodological rules**, every one paid for by a planner
  registration defect that is recorded rather than repaired away: state
  designed-null aggregation rules in advance; pre-state the target's
  measurable scale and the design's MDE (a noise-floor null is UNDERPOWERED,
  not a null); verify a non-zero causal channel at every tested parameter
  value (an inert knob's null is VACUOUS); bound MATERIALITY via equivalence
  forms, never nil significance on a known-nonzero quantity; justify the
  analysis grain for power rather than inheriting it; define the winner
  jointly over target AND safety, never by the target's extremum; and where
  the safety check has graded levels, report the best arm at each.

## Neither deployed nor shelved

Both certified repairs remain UNADOPTED, deliberately. Changing a frozen
operator creates a NEW operator under F16 and requires its own study ID and
seal. Before that decision is worth making, the repairs need a generalization
test beyond the three M4-E2 worlds they were measured on — which is the next
registration.

## Scope

Everything here is synthetic, calibrated to the real-text regime, at
EXPLORATORY tier. It licenses design priors, refusals, and a repair candidate
for a frozen operator. It licenses no claim about any corpus, construct,
person, or diagnosis.
