# SUICA M4-C Response-Safe Condition-Manifold Discovery

Date: 2026-07-31  
Status: synthetic discovery route; psychological interpretation closed

## 1. Development problem

M4-B demonstrated that opportunity selection, opportunity creation, return,
recovery, and history-conditioned feedback can be separated in finite
synthetic worlds. Its condition categories were nevertheless planted by the
generator. M4-C removes that remaining dependence.

For event \(e=(u,o,t)\), define the pre-response sigma-field

\[
\mathcal F_e^-
=
\sigma(E_t,\text{current menu descriptors},\text{incoming context},
\text{prompt/provenance},\text{source/time}).
\]

The condition chart must obey

\[
\widehat\zeta^{(-u)}
=
\mathcal A\left(\{W_i^-:u_i\ne u\}\right),
\qquad
\widehat z_e=\widehat\zeta^{(-u)}(W_e^-).
\]

It may not use author identity, current choice, current or future response,
future menu, mechanism truth, Big5, MBTI, or any evaluation outcome.

## 2. Object

The object is not a named topic axis. It is a local atlas or an equivalent
condition-relation representation:

\[
\widehat{\mathcal M}
=
\{(\mathcal U_a,\zeta_a),T_{ab}\}.
\]

Coordinate rotations, signs, and individual matrix elements are not formal
outputs. The stable outputs are neighborhood relations, geodesic/landmark
geometry, overlap transport, support coverage, and the action of a frozen
conditional-response operator on held-out conditions.

The V1 implementation registers three response-free candidate families:

1. robust scaling plus linear PCA;
2. robust scaling plus global Isomap;
3. a k-nearest-neighbor geodesic atlas represented by frozen landmarks.

Each source is fitted separately. Corresponding reference landmarks turn
source-specific coordinates into a common radial basis:

\[
\phi_{s\ell}(z)
=
\exp\left[
-\frac{d_s(z,\ell)^2}{2\sigma_s^2}
\right].
\]

This avoids interpreting arbitrary axis alignment as condition recovery.

## 3. Evidence split

The route separates:

- reference calibration: fit scaling, graph, and candidate charts;
- reference selection: choose a chart using pre-response geometry only;
- mechanism calibration: fit the conditional-response head;
- mechanism selection: choose response regularization;
- untouched mechanism evaluation: evaluate once.

The chart-selection objective combines cross-source relation geometry,
cross-author-half stability, trustworthiness, continuity, support coverage,
and a fixed complexity preference. No response metric enters this choice.

## 4. Registered attacks

The synthetic battery contains:

- an identifiable linear condition surface;
- a nonlinear closed surface requiring local treatment;
- source-specific rotations;
- author fingerprint leakage;
- current-response leakage;
- a branched topology conflict;
- an unstable no-manifold condition representation; and
- a noninjective condition alias whose quotient geometry cannot identify the
  response law.

Permutation of every response while holding pre-response features fixed must
leave the selected chart and all chart features unchanged to numerical
tolerance. Leakage, unsupported geometry, topology mismatch, and condition
alias are successful refusals rather than failed predictions.

## 5. Frozen response test

After chart selection, the discovered landmark basis is used in an
author-specific ridge response surface:

\[
\widehat G_u(z)
=
\widehat a_u+\widehat B_u\phi(z).
\]

Regularization is selected on the mechanism-selection panel and evaluated on
the untouched panel. Comparison with the generator's noiseless response gives
an oracle-retention diagnostic:

\[
R_{\mathrm{retain}}
=
\frac{R^2_{\mathrm{discovered}}}{R^2_{\mathrm{oracle}}}.
\]

This tests one conversion edge from discovered condition geometry to
conditional response. It does not yet re-estimate every M4-B ecology operator.

## 6. Identification and refusal

Chart recovery requires local injectivity or an explicit quotient claim,
reference support coverage, shared landmarks/overlap across sources,
author-independent inputs, and stable graph geometry. The system refuses when:

- provenance exposes response or author information;
- author fingerprints dominate pre-context;
- cross-source or author-half geometry is underresolved;
- evaluation conditions leave reference support;
- a hidden condition alias preserves chart appearance but destroys response
  retention; or
- topology is forced into an incompatible global chart.

An augmented predictive-state chart that includes past responses is a
different object and must not be silently relabelled as this condition chart.

The V1 observable mechanism-refusal rule is fixed after chart selection:

\[
R^2_{\mathrm{conditional,heldout}}<0.60
\quad\Longrightarrow\quad
\texttt{MECHANISM\_UNDERRESOLVED}.
\]

This rule does not diagnose *why* the response is underresolved. Synthetic
truth is used only afterward to verify that the registered alias world is one
such cause; the operational refusal itself does not read the alias label or
oracle surface.

## 7. Claim boundary

M4-C can at most establish finite synthetic recovery of anonymous condition
geometry and preservation of a registered conditional-response mechanism.
It cannot name topics, infer personality, emotion, cognition, diagnosis, or
clinical meaning. Full chart-covariant transport of M4-B selection, creation,
return, recovery, and history-gate operators remains a later development
stage.

## 8. Discovery result

Eight repetitions across eight registered finite worlds gave:

| Diagnostic | Result |
| --- | ---: |
| identifiable-world resolution | 22/24 = 0.917 |
| held-out geodesic-distance Spearman | 0.913 |
| held-out 10-neighbour Jaccard | 0.774 |
| cross-source relation geometry | 0.963 |
| conditional-response oracle retention | 0.925 |
| author-response surface geometry | 0.960 |
| author/response/no-manifold refusal | 1.000 |
| condition-alias mechanism refusal | 1.000 |
| response-permutation invariance | 1.000 |
| branched topology resolution/refusal | 1.000 |
| registered topology-label match | 0.833 |

Decision:
`M4_C_CONDITION_MANIFOLD_DISCOVERY_PASS`.

The pass applies to the V1 estimand only. One nonlinear-surface cell missed
the neighbour gate and one source-rotation cell missed the geometry gate.
Circle topology was correctly labelled in only 4/8 source-rotation cells.
Thus the topology signature is a refusal/diagnostic layer, not a theorem of
general topology reconstruction. The branched attack was detected after
global-chart selection rather than automatically selecting an atlas.

The reference calibration and reference selection panels contain independent
observations from the same reference authors. They are separate from the
mechanism authors, but this is not yet strict author-fold cross-fitting.
Response permutation proves that the fitter does not read the separately
stored response tensor; unlabelled response leakage hidden inside a
misdeclared pre-context field remains a provenance threat.

## 9. Complete-ecology seam

M4-C.2 replaced finite category one-hots in the complete M4-B ecology with the
frozen chart basis:

\[
o_{u,t}=\int\phi(z)\,d\nu_{u,t}(z),
\qquad
c_{u,t}=\phi(\widehat z_{Q_t}).
\]

Its corrected V2 confirmed exact matched paths, truth-open condition-alias
information loss, source/null controls, gauge safety, and atomic choice,
creation, return, and recovery transport. It did not close the complete seam:
composite loop geometry was `.652` and only `2/8` repetitions passed all
gates. The result is `M4_C2_NO_GO_CHART_TRANSPORT`.

M4-C.3 therefore decomposes the physical loop into creation, response, and
choice edges, constructs all eight oracle/discovered products, and compares
the Jacobian product with a nonlinear finite intervention. This can localize
the loss but cannot retroactively change V2. M4-D remains blocked because
otherwise composition error and dynamic-generator error would be confounded.
See `docs/SUICA_M4_CHART_COVARIANT_ECOLOGY_THEORY.md` and
`docs/SUICA_M4_PHYSICAL_EDGE_COMPOSITION_THEORY.md`.
