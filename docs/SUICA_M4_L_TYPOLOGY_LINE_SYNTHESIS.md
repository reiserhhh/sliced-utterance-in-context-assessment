# SUICA M4-L — The Typology Line, Synthesized

Status: **line closed 2026-08-10**, three executed legs (L1, L2, L3) on
the IDT-R theory (appendices P/Q/R/S). Tier: EXPLORATORY, label-free,
synthetic, card-space. The claims ledger controls. Companions:
`docs/SUICA_IDENTITY_THEORY_V1.md` (P–S),
`docs/SUICA_M4_L_TYPOLOGY_LINE_PLAN.md` (every registration, outcome,
adjudication).

## 1. The question

The program owner's reverse reading of IDT: does "error = 0" assert "no
identity", and would REMOVING identity make correct personality grouping
possible? Formalized as the type layer x = μ + τ_g + b + s + ε, with
identity b now the INTERFERENCE and type τ the signal — the
complementarity principle's fourth instantiation.

## 2. What was measured

| leg | question | answer |
|---|---|---|
| L1 | the poles: does the typed world behave, and do the R-theorem shadows hold? | V-1/V-2 HOLD (error-free world groups at 0.995; identity tax real, ordering exact cell-for-cell); the ALIGNED/ISO tax ratio is **8.15×** at matched energy; projection restores only 13.5% (my bar was on the wrong quantity — the floor-invariance lemma was born here); the audit meter tracked 5/8 with one diagnosed constant. |
| L2 | the threshold, the continuum, the calibrated audit | **The η-floor curve is MEASURED** (7/10 + exact ordering, both energies). The localization mechanism is real (+0.257, 4.28× at the hardest rung) but its qualitative window is EMPTY at these dims (the window lemma; my two window conditions were jointly unsatisfiable — rule 20 was born here). Q.5 refuted: the audit's error is partition-borne. |
| L3 | the taxometer and the propagation-corrected meter | **η̂ certified at ±0.125** (10/10, median error 0.024, ordering exact under every reading; +O(0.05) pole bias; whitening-shape precondition). The propagation law holds (R² 0.89, oracle anchor inside); the corrected meter is accurate to ≤0.005 absolute and the per-world constant fluctuates at that same scale — the meter's noise floor, where the chase ends by measurement. |

## 3. The result

The owner's conjecture, finished (appendix S): **identity removal DOES
enable correct grouping — conditionally, and every condition is now a
theorem or a measurement.** True removal works (0.995) but is circular
(R1 → EM); label-free surrogates cannot buy the boundary-normal
component (floor-invariance lemma) and the floor curve σ_u²(η) governs
(measured three times); the applicable-or-not question is itself
readable (the certified taxometer); and "error = 0" is an AUDIT with a
calibrated meter (precision ±0.005), under which typology in an
identity-bearing world fails by exactly the identity share. The
types-vs-dimensions debate is, in this family, an instrument reading.

## 4. Method

Defects #27–#38 (twelve, all planner, all recorded), producing standing
rules 18–21: joint satisfiability; shadow fidelity; stop-on-empty;
precision budgets. Three legs, three P2-class verdicts — and in each
case the mechanically-fired pessimistic reading was corrected by graded
adjudication into something sharper than the registered optimistic
branch would have been: the floor-invariance lemma, the window lemma,
and the certified-with-known-bias taxometer are all children of MISSes.
Executor behaviors of record: L2's joint-satisfiability check proving
the planner's window empty before spending arms; L3's disclosure that
its first Part-0 run mis-implemented two clauses, corrected before any
arm, the defective verdict disclosed rather than overwritten.

## 5. What is open

1. **The window-width conjecture** ((d/n)^¼ scaling of the
   localization window) — named, not queued; a natural D1 seal item.
2. **Dense/heterogeneous-gallery regimes** — both this line and K3's
   crowding replacement live in sparse near-homogeneous worlds.
3. **Real-text transport** — as everywhere in the program: these are
   instrument-world laws at EXPLORATORY tier; no shared-occasion,
   typed real corpus exists; stated as a limitation, not a queued test.

**Dated stamp (2026-08-10):** the L-line rows of the headline table were
adversarially verified in defense leg D2: floor law and taxometer rows
QUALIFIED on wording only (honest grading in IDT appendix T), 0 REFUTED.
