# SUICA M4-M — The Level-Law Completion Line — SYNTHESIS

Line opened 2026-08-11 (`140e927`), closed 2026-08-11 (M3 adjudication).
Seven legs, all registered before run, one commit each, never amended,
never pushed by agents; suite 970/970 at every commit. Tier:
EXPLORATORY, synthetic, instrument-world laws; the claims ledger
controls; NOTHING here licenses claims about corpora, constructs,
persons, or diagnoses.

## The question, and what it became

K2f left the restored level law as "an intercept minus the tax,"
exponent unidentifiable (designed collinearity −0.964). The line asked
the named follow-up — decollinearize and identify — and the question
transformed three times under measurement: from "what is q at level"
(M1c: sign-negative → M1d: q dissolves into a parameterisation-free
negative slope) to "what carries the shape" (M1e: a steep negative
r-power, not additive state-dynamics) to "what is the tax" (M3: a
declining quadratic curve, not a constant).

## The laws (grades are part of the claims)

1. **The scoped level law — PREDICTIVE-SCOPED** (M2, `a53d10d`):
   field(share, φ) = α(share) + λ·r^q with λ =
   −0.057625974791364554 [−0.0843564122153383,
   −0.042724477794351616] identified and q UNQUOTABLE (rule 27; CI
   [2.05, 5.92]); predictions (not parameters) sealed-then-hit 3/3 at
   share-exterior, φ-exterior and interior-new-φ configurations with
   realized r interior to [0.4541409476972356, 0.8189581462487876].
   Zero pre-stamp world generations; salt embedded.
2. **The tax curve — LEVEL-grade, interpolative in V ∈ [0.03, 0.21],
   closure-validated, consumable** (M3, `1b0d330`): α(V) = c − κ0·V +
   (κ2/2)·V² with c = 0.21247398265278816 [0.2089174011834685,
   0.21623033411776654], κ0 = 0.9601680204204508 [0.8935297536704152,
   1.0313114338425122], κ2 = 1.562877770472943 [1.0324533419318935,
   2.119753814549891]; local tax κ(V) = κ0 − κ2·V declines 0.913 →
   0.632. Form vs A-sat UNSETTLED beyond V = 0.21 (3.2% tie).
3. **The retrodiction closure** (M3): one law retrodicts 5/6
   published κ̂ values through each estimator's OWN pipeline
   (0.722→0.746, 0.750→0.768, 0.760→0.763, 0.777→0.781, 0.676→0.680
   with the pre-signed LOW mechanism holding; miss: the 9-pair refit
   0.715→0.749, 0.0045 over a 0.03 point-tolerance). The
   representation spread of κ is thereby EXPLAINED, not adjudicated
   away.
4. **The re-attribution** (M1c/M1d, appendix X.2/Y.2): the
   response-grade "quadratic over-response" band (q = 1.83
   [1.71, 1.98]) was the variance tax wearing r's clothes — no
   V-clean positive-exponent measurement exists; the V-omitted shadow
   fit on the decollinearized corpus snaps to q = +2.245
   [2.177, 2.319]. Yield clause stands.
5. **The readability penalty** (M1e, appendix Z.1): the r-channel is
   small, steep, NEGATIVE — the level price concentrates where cards
   read best; additive state-dynamics representations lost twice.
6. **Cross-corpus transfer** (M1d): with NO refit, the
   constant-minus-tax-with-weak-negative-slope structure retrodicts
   K2f's 26 legacy rows at 18.9× the sealed composite's accuracy.

## Dead or withdrawn, by measurement

"q = −0.150" as a structural exponent (parameterisation artifact of
the missing constant; the invariant is the slope sign); the naive
φ-power representation (lost even to incumbents); additive
separability of the field (11.9% LOO margin against); the constant
tax; M1's marginal-correlation feasibility gate (unsatisfiable by
construction — infimum 2.5× its own bar).

## The method arc (what the line paid and what it bought)

Defects #43–#49 (six planner, one predicate-domain) bought rules
25–29: **25** feasibility gates in the estimand's quantity (proxies
report, never gate) — validated in both directions within one leg;
**26** active-bound winners co-adjudicated unbounded; **27**
identification budgets on consumed objects (selection ≠
identification — its first firing blocked a 3.87×-over-budget
exponent while the REJECTED model met every budget); **28**
representation licensing for cross-leg comparisons; **29**
regime predicates pinned in the statistic's own domain (the M2
saturation call: the statistic's null is not its floor; everything
turned on one word, and the registered word won). Conventions:
planner RUNS all registration-time arithmetic and embeds the tables
(#43); routing tables mechanically verified disjoint-and-covering
(#46). M1b measured the required budget instead of excusing a mushy
fit (192 worlds/cell); M1c's headline was corrected by M1d within one
day, by dated note, never rewrite.

## Open, named, not queued

The exponent q's value (needs a wider r-window at fixed V — an
instrument-side knob question); A-quad vs A-sat beyond V = 0.21 (the
extrapolant question; A-quad's zero at V ≈ 0.614 is out-of-scope
arithmetic); the target-2 forensic (9-pair weighting); the α(0.70)
triangle (exterior-share channel bias); the within-share concavity's
final owner at sub-SEM scales; everything real-text (governance
R-G1..R-G8, untouched). The MECHANISM of the quadratic tax is
chartered as the M4-N line (appendix CC's square-agreement
derivation; N1 seals its response-transport prediction).

## Ledger of commits

`140e927` charter + M1 registration · `ac66414` appendix W (pre-run
derivation) · `58bb036` M1 STOP_DESIGN_INFEASIBLE · `2e4e404` M1
adjudication + M1b registration · `dde8efd` M1b NON_PROJECTABLE ·
`dd1a38f` M1b adjudication + M1c registration · `9a625e5` M1c
LEVEL_RESPONSE_DISSOCIATION · `54afc77` M1c adjudication + appendix X
+ M1d registration · `3ad07ee` M1d COMPLETED_BUT_INCOMPLETE ·
`af4a335` M1d adjudication + appendix Y + M1e registration ·
`bbd842f` M1e IDENTIFIED_INSUFFICIENTLY · `97041cd` M1e adjudication
+ appendix Z + M2 registration · `a53d10d` M2
LEVEL_LAW_PREDICTIVE_SCOPED · `d552bd5` M2 adjudication + appendix AA
+ M3 registration · `1b0d330` M3 TAX_IS_A_CURVE · (this commit) line
close.
