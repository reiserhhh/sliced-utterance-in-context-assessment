# SUICA M4-N — The Tax-Mechanism Line — SYNTHESIS

Opened 2026-08-11 on the M-line's close; closed 2026-08-14. Tier:
EXPLORATORY, synthetic, instrument-world laws; the ledger controls;
no claims about corpora, constructs, persons, or diagnoses.

## What the line established

1. **The tax curve TRANSPORTS** (N1b, `8fb53fe`): fitted at level,
   it predicted response-grade measurements it never consumed —
   sealed-then-hit 3/3 (κ̂_HI exact to 8.3e-5; the decline Δκ̂ =
   0.251 with CI excluding zero). Grade: **PREDICTIVE
   (response-transport), V ∈ [0.03, 0.21]**. The constant tax is
   dead in both spaces by sealed measurement; the sealed 0.722 is a
   secant of the curve (V̄ ≈ 0.15, approximate).
2. **The closure is clean** (N4, `10142c5`, adjudication dated note):
   5 hits, 1 WITHIN-NOISE (−1.37 SE of the target's own estimator;
   χ² at expectation), 0 discrepant — the law is unrefuted by all
   six legacy κ̂ targets. The one "miss" was a tolerance typed
   narrower (1.19 SE) than the target's own sampling noise.
3. **The mechanism candidate stands, once-tested:** square-agreement
   (α = c′ + A0(1 − V/V*)², appendix CC.1′ at executed provenance)
   survives its transport test; its remaining questions are
   unposable here — N2 (the c′ anchor) DISSOLVED by derivation
   (pure in-support reparametrization; vertex unreachable ×2;
   degenerate-truth protocol undefined), N3 (quad-vs-sat) held
   NAMED with executed feasibility arithmetic (max separation
   0.0018 at share 0.95; ≈ 3841 worlds/cell for 3 SE in an
   unexercised regime).
4. **Micro-discrepancy dissolved:** N1b's low-V̄ residual (+1.31 SE)
   and N4's lowest-V̄ pair gap (opposite sign, |z| < 1.8) bracket
   zero.

## The method arc

Defects #50–#54, all planner's, bought rules 30–32: **30** executed
provenance for every published derived constant (the machine caught
the planner inventing digits — 16-digit identities consistent with a
κ-pair existing in no artifact); **31** two-sided reachability for
every verdict bar (a bar that cannot fail adjudicates nothing;
residual-defined components never count toward attribution); **32**
estimator-noise floors for forensic gaps (gaps in SE units;
within-noise routes to WITHIN-NOISE). Conventions extended:
ladders checked against every gate sharing the knob (#51);
routing-consequence entailment added to the #46 mechanical check
(#54). Executor-side: the FORM_SPLIT hard-code self-catch (RN-N1B-10)
recorded as #50's mirror — the audit machinery corrects both sides
of the division of labor.

## Open, named, not queued

N3 with its executed table (a share-0.95 regime argument is the
price of entry); the frame-floor question in any future generator
extension (owner-scale); the readability penalty's mechanism — the
successor M4-P line (`docs/SUICA_M4_P_PENALTY_MECHANISM_LINE_PLAN.md`).

## Commits

`76060e7` charter + N1 registration · `487f1d8` N1 STOP · `6eeec0d`
N1 adjudication (#50/#51, rule 30) + N1b registration · `8fb53fe`
N1b CURVE_TRANSPORTS_TO_RESPONSE · `38b7614` N1b adjudication +
appendix DD + N4 registration · `10142c5` N4 · (this commit) N4
adjudication (#52–#54, rules 31–32), N2 dissolution, N3 feasibility
table, line close.
