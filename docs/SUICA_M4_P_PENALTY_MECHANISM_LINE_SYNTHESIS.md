# SUICA M4-P — The Penalty-Mechanism Line — SYNTHESIS

Opened 2026-08-14 on the N-line's close; closed 2026-08-14. Tier:
EXPLORATORY, synthetic, instrument-world laws; the ledger controls;
no claims about corpora, constructs, persons, or diagnoses.

## The question and the answer

WHY does the level price concentrate where cards read best (the
readability penalty, appendix Z.1)? Answer, built in five legs: the
deployed relation-field gauge's b-only signal is FRAME-BORNE —
injected frame content moves the statistic 96–97% through
frame-vs-frame agreement (P2: f = 0.9584700070215529 /
0.971270002747466), the natural φ-gradient is ~88% frame-owned
(P3c: D_grad = 0.00853 [0.00528, 0.01195] of range_nat = 0.00964
[0.00716, 0.01223]; transportable share 0.115 [−0.108, 0.347],
UNBUDGETED), and the gauge's reading scored against a DIFFERENT
frame's truth is near zero at every φ (R_refresh −0.0009/−0.0014/
+0.0002 vs R_nat 0.121–0.131; the dropped-sub-case note, #58).
**The instrument statement: at this family's regimes, the gauge is
predominantly a frame-agreement instrument; genuine cross-frame
person transport is at most about a third of the gradient (CI),
point about a ninth.** The M/N-line laws are UNTOUCHED as laws of
the statistic — their sealed predictions predicted the statistic
and still do; what changed is the mechanism section: the r-channel
reads frame-agreement.

## What the line built

- **The sign and its decomposition** (P1 + P2): scaffold(+) at
  32–35× the bar; then f = 0.96–0.97 — P1's magnitude re-typed ÷20
  by dated note; K-R1's F-1 caveat became a number; the coherent
  picture (reading is frame-mediated; the mediation's marginal
  value is small; the statistic co-moves with frame on both sides).
- **The instrument theorem** (P3): generator-side frame refreshment
  is impossible on the published k2b interface (exhaustion over its
  2-parameter input space); every published refreshment was
  reader-side — T9's counter-operation had two layers and only one
  existed (appendix HH).
- **The certified split-seed instrument** (P3b): 4-edit extraction
  from k2b:321-349 with an 18-entry provenance table; author half
  bit-exact vs k2b at equal seeds; C2 battery + a live M1c anchor
  (C1′ 3/3 in P3c). A standing program asset.
- **The estimand lessons** (P3b/P3c): ratios with 3-SE denominators
  are unmeasurable at any feasible size (the difference estimands
  are the licensed form); INHERITANCE IS NOT EXEMPTION (#56);
  pilot correlations are never consumed (#57); estimand pivots
  carry every still-measurable sub-case (#58).

## Defects and conventions

#55 (classification disjointness — NULL-first pinned), #56
(feasibility inheritance), #57 (pilot second moments), #58
(sub-case carry-forward) — all planner's, all non-blocking, each
now a convention. Rules unchanged at 1–32; the registry carries
the dated notes.

## Open, named, not queued

P3d (NULL certification at ≈ 2,470 pairs/φ — one label for 3.2×
compute); the tax curve under refreshment (the split instrument
enables it); reader-side vs generator-side refreshment compared on
one design; the M4-Q transport line (registered:
`docs/SUICA_M4_Q_TRANSPORT_LINE_PLAN.md`) — the same device pointed
at the CARD readers, completing the instrument-role taxonomy's
mechanism.

## Commits

`7589299` charter + P1 · `ee34c74` P1 SIGN_SCAFFOLD · `b61cc52` P1
adj + FF + P2 · `f8110d6` P2 GENUINE_SCAFFOLD · `caba52f` P2 adj +
GG + P3 · `f86ab90` P3 INFEASIBLE_SPLIT · `debc68e` P3 adj + HH +
P3b · `6cd3bdb` P3b NON_PROJECTABLE · `11f42d6` P3b adj + #56 + P3c
· `e92c31a` P3c UNDERPOWERED · (this commit) close.
