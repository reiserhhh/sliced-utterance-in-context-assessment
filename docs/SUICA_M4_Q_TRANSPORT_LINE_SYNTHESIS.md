# SUICA M4-Q — The Transport Line — SYNTHESIS

Opened 2026-08-14 on the P-line's close; closed 2026-08-14. Tier:
EXPLORATORY, synthetic, instrument-world laws; the ledger controls;
no claims about corpora, constructs, persons, or diagnoses.

## What the line established

1. **The two instrument layers read DIFFERENT TARGETS** (Q1,
   `5c839f2`): the card statistic's truth is the frame-free trait;
   the gauge's truth carries the frame. Appendix N's 4.5× is a
   different-targets comparison — **rule 33**: instrument
   comparisons are licensed only between readers of the same
   registered target. The stop was proven (byte-for-byte zero-point
   identity first, degeneracy second), with a free third
   replication of the gauge collapse (gap 0.129).
2. **The card is pure trait** (Q1b, `ae2864f`): against the SHARED
   component (the centred trait — defect #60's lesson), the
   disattenuation identity holds to ±0.0004 at both φ. Card errors
   are frame-independent. And structurally: this generator's cards
   contain NO non-trait author channel — the founding question
   ("does the card carry identity beyond biography?") is UNPOSABLE
   here (appendix KK). Not false: unposable.
3. **The tax is frame-borne** (Q2, `2e11c21`, zero defects): R_ref
   flat at zero at both V-endpoints while R_nat falls 3.8×; D_ref
   NULL [−0.0054, +0.0010] against D_nat = 0.1196 — which the
   M-line law PREDICTED at z = 0.52 (the law's first use as an
   instrument). With P2/P3c, every measured face of the gauge's
   statistic — level, φ-gradient, V-response, injection response —
   is frame-agreement. **The instrument statement completes: the
   N-line curve is the frame channel's V-response.**

## Defects and method

#59 (definition-level degeneracy — estimand contrasts and cell
antecedents now carry non-degeneracy proofs) and #60
(reference-object — disattenuation identities name their shared
component), both planner's, both computable at registration, both
caught with zero measurement cost. Rule 33 enacted. Q2 closed
clean. The executor's dual-reading pins (RN-Q1B-6, RN-Q2-1)
adjudicated two legs correctly before any number existed.

## Open, named, not queued

Q3 (the two refreshment layers head-to-head); the a_load channel's
role outside the card path; everything the M4-R line now owns.

## Commits

`0f3d773` charter + Q1 · `5c839f2` Q1 INEXPRESSIBLE · `527d176` Q1
adj + JJ + rule 33 + Q1b · `ae2864f` Q1b · `d030914` Q1b adj + KK +
#60 + Q2 · `2e11c21` Q2 TAX_FRAME_BORNE · (this commit) close.
