# SUICA M4-R — The Identity-Channel Line — SYNTHESIS (measurement phase)

Opened 2026-08-14 on the Q-line's close; measurement phase closed
2026-08-14. Tier: EXPLORATORY, synthetic, instrument-world laws; the
ledger controls; no claims about corpora, constructs, persons, or
diagnoses.

## What the line built and answered

1. **The identity channel exists and is certified** (R1 `728fae4` +
   R1b `8632434`): a per-author non-trait style vector entering the
   card-visible response path via the trait_eff identity (no k2b
   edit), inert at zero BIT-EXACTLY, author-stream,
   trait-independent, with monotone dose matching the composition
   arithmetic to 1% and a prospective containment hit at the fresh
   dose w = 0.75 — where the #61-corrected band was proven
   LOAD-BEARING (the old band fails the same data at −1.397).
2. **The gauge meets identity** (R2 `22d9256`): interference REAL
   (ΔR_T = −0.0062, CI excludes zero) but 11.65× below the curve's
   sealed price; the frame-controlled contrast (now the registered
   form, #63) delivers the gauge half of the founding answer —
   identity fully readable within frame (+0.0305), erased by one
   frame refresh (−0.0024).
3. **The tax is channel-specific** (R2b `e06d73f`): three sealed
   hits — the curve holds on its own (slow) channel at position
   +0.058; the mu-channel tax's first interval 0.0747; separation
   D = 0.817 (ratio 11.93×) explaining R2's mispricing as the
   channel ratio. The M/N laws gain their channel-scope clause by
   dated note; nothing else moves.

## The founding answer

The residual-as-ID conjecture, on this family: **identity lives in
cards, transports across frames, is cheap for the field-reader to
carry and impossible for it to read across frames.** (Adjudication
of R2b, items 1–5.)

## Defects and conventions

#61 (prediction bands carry derivation error; deterministic points
get equivalence — proven load-bearing in R1b), #62 (channel
coverage named), #63 (frame-controlled forms for cross-frame
claims), #64 (sign conventions with worked examples) — all
planner's; the executor caught every one before consequence, twice
pre-stamp on sign-flipping ambiguities.

## Open, named, not queued

**R3 held for a planner derivation**: the typology-η identity and
the style-channel identity are different formal objects; the
reconciliation derivation precedes any registration. Also named:
the mu-tax's own dose curve; style×slow interaction; T6″ against
planted style at the generator layer.

## Commits

`f8bc446` charter + R1 · `728fae4` R1 INSTRUMENT_DEFECT(C-R1c) ·
`dec5557` R1 adj + #61 + R1b · `8632434` R1b
IDENTITY_CHANNEL_CERTIFIED · `49b5161` R1b adj + R2 · `22d9256` R2
INTERFERENCE_MISPRICED · `4bece27` R2 adj + #62/#63 + R2b ·
`e06d73f` R2b TAX_IS_CHANNEL_SPECIFIC · (this commit) phase close.

## Dated addendum (2026-08-19) — R3 discharged and sealed; the line closes fully

The reconciliation derivation
(`SUICA_R3_IDENTITY_RECONCILIATION_NOTE.md`) resolved the two named
identities into a 2×2 (geometric alignment × channel semantics) with
η̂ formalized as an excess-alignment reader, and the R3 leg sealed its
composition law parameter-free: η̂(η₀,w) = η₀/(1+w²), 8/8 registered
predictions inside the certification band, both energies, instrument ≡
oracle to ≤ 0.041. The first forgery direction is measured (a typed
world driven to η̂ 0.12 by style with geometry unchanged); the signed
dissociation exists (shown by the declared second reading; the
registered reader saturated — defect #84a); the whitening precondition
is structurally IMMUNE to card-level style (the bound the planner
registered was in a blind coordinate — #84b; the immunity is the
better theorem). The program's dissociation family now has both its
formal home and its instrument-level seal. Defect #84; suite 1304.
**The R-line is fully closed.**
