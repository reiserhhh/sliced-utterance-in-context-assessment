# SUICA M4-S — The Selection Line — SYNTHESIS

Opened 2026-08-16 by the owner's direction (synthetic + real);
closed 2026-08-16. Tier: EXPLORATORY throughout; corpus-level only
on real data; the ledger controls; no person claims anywhere.

## The conjecture and what became of it

The owner's conjecture: the gauge reads frames — but WHICH frames a
person chooses is person-owned, so selection-proximity should imply
personality-proximity. Adjudicated as two claims:

- **(a) Selection is a person-stable signature.** REAL DATA, strong:
  1306 PANDORA users, split-half self-cosine 0.681 vs 0.051
  permuted; pairwise-similarity ceiling 0.733 (SR0, `1009c97`).
- **(b) Selection-similarity ⇒ trait-similarity.** NOT a theorem —
  M4-S1's γ = 0 arm built a world whose selection signature is
  exactly as stable (0.556 vs 0.563) and carries ZERO trait
  information (Mantel −4e-05); the falsifier reproduced on fresh
  worlds in S2. (b) must be measured. **It was: DETECTED on
  PANDORA** — Mantel r = 0.048987613136188025, permutation
  p = 0.001 (z = 5.42) over 852,165 pairs, positive in all six
  registered geometries, both time halves, all three activity
  tertiles; disattenuated ≈ 0.064 under declared reliabilities
  (SR1, `c6a2335`). Config-before-joint seal proven; zero ID
  leaks; body never read.
- **The synthetic shape law** (S2, `a4a1f2d`): r(γ) follows
  g(γ) = γ²/√(γ⁴+(1−γ)⁴) in FORM, with the analytic coefficient
  systematically overshooting the softmax pipeline; the
  pipeline-corrected co-stamped prediction lands 3/3 in bands
  8–40× tighter. The law of record is the corrected shape; the
  analytic g is its idealization.

## The mechanism reading

With the P/Q-line instrument statement in force (the gauge is a
frame-agreement meter), selection is the licensed route by which a
context meter becomes a person reader: **people carry themselves
into pairwise geometry through the frames they choose**, even
where the gauge's own reading cannot cross frames. The rind
model's choice channel — the program's founding design principle —
returns with a mechanism, a falsifier, a real-data pairwise
detection, and a sealed shape.

## Method

Defects #65–#67, all planner's, each buying a convention
(neutrality naming; permutation-power method pinning; bias-vs-band
category discipline with dual stamping). The executor's
contributions this line: the dual-reading pin that saved S1, the
SR0 no-peek gate hardened against its own false positive, the SR1
robustness table, the S2 co-stamp. Governance held throughout:
aggregates only, zero leaks in three scans, Essays and native
corpus untouched.

## Commits

`21149bb` charter + S1 + SR0 registrations · `da47a0e` S1 ·
`131cc5d` S1 adj + #65 + S2 registration · `1009c97` SR0 ·
`5d5569d` SR0 adj + SR1 registration · `c6a2335` SR1 ·
`84962a5` SR1 adj + #66 + appendix NN · `a4a1f2d` S2 · (this
commit) S2 adj + #67 + close.
