# SUICA M4-T — The 4W / Hierarchical-Selection Line — SYNTHESIS (measurement phase)

Opened 2026-08-17 by Codex's T1 and the owner's 4W synthesis;
measurement phase closed 2026-08-17 under the loop discipline.
Tier: EXPLORATORY throughout; label-free except the stage-E legs
(SR2–SR4, labels only inside stamped joint stages); corpus-level
only; no person claims; the ledger controls.

## The arc

1. **T1 (Codex, adopted):** selection deviations form nested
   conditional innovations (4–5 reproducible levels, triple-gated)
   plus a tail no tree exhausts (residual AUC 0.9552; honest
   EXPLORATORY/NOT-PREREGISTERED typing upheld).
2. **T2 (`acfd2ef`):** the tail TRANSPORTS — early taste predicts
   never-before-used communities (AUC 0.6031, ablation-robust
   0.5941); strict within-leaf matching is unresolvable on this
   corpus (defects #68/#69 bought the composite-null and
   pool-target conventions).
3. **T3 (`7a899d2`):** the identity budget — taste dominates (82%
   of 5.957 bits; conditional 0.9311 > support's marginal ceiling
   0.7294, the dominance argument), support moderate, the tree
   subsumed by its own leaves (0.506), a 3.9-point gap remains
   (typed capacity — CORRECTED by SR4: predominantly metric).
4. **SR2 (`b98e22e`):** the coupling budget — SR1's r = 0.049
   SURVIVES the observability audit (z = 5.13); observability
   itself is trait-NULL; the taste centroid is not a surviving
   carrier; 23-community ablation costs 41%. **THE DISSOCIATION:
   identity's dominant carrier is largely trait-silent** — S1's
   γ = 0 lesson inside the real budget.
5. **SR3 (`a148c7b`) + SR4 (`99eb4c2`):** the carrier localized —
   no continuous carrier at ANY rank (capacity exhausted by the
   5.3e-15 rotation proof); **METRIC_BORNE**: 87.6% of the
   coupling gap and 61.2% of the identity gap live in what PPMI
   suppresses and Hellinger keeps — the common-community mass.

## The line's proposition

**Identity lives in the distinctive; personality lives in the
common.** Who you are (re-identification) rides a low-rank
generalizing taste coordinate; what you are like (Big5 coupling)
rides your allocation over widely-shared opportunities. The two
live in different strata of the same selection behavior, and the
compression that optimizes one discards the other.

## Method

Defects #68–#75, all planner's, all conventions now standing
(composite-null predicates; pool targets; transform pinning; band
projections; pair-set alignment; verdict-divergence flags + the
A1 stamp-order hardening; sweep endpoints vs available rank;
effect-size-keyed cells). The executors' contributions: T1's
frozen-tree machinery; the bit-exact reconstruction discipline
(SEED+1000*fold); the context-exhaustion honest stop; the
dominance observation; the A1 failed-G0-cannot-stamp hardening;
the recompute-not-copy anchor practice; three self-corrections of
drafted prose against artifacts. Suite 970 → 1021; zero ID leaks
across every scan.

## Open, named, not queued

When-dynamics (4W §2.4 — the unposed quadrant); second-corpus
branch transport; common-mass refinement at cluster level (naming
governance applies); R3's reconciliation; the flat-vs-joint
identity gap's metric refinement; the owner's parked decisions
(off-machine copies; F16 ×2; F12/F13; native corpus).

## Commits

`d9da17d` T1 (Codex) · `24d4a80` 4W theory (owner) · `4e9e339`
adoption + T2 registration · `acfd2ef` T2 · `5bbf846` T2 adj + T3
reg · `7a899d2` T3 · `a34f12c` T3 adj + SR2 reg · `b98e22e` SR2 ·
`4af9442` SR2 adj + SR3 reg · `a148c7b` SR3 · `9ba3cf1` SR3 adj +
SR4 reg · `99eb4c2` SR4 · (this commit) SR4 adj + phase close.
