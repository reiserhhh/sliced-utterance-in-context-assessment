# SUICA M4-U — the When line: synthesis of the measurement phase

Closed 2026-08-18. Four legs under the loop discipline (registration
committed before every run; one commit per leg; suite 1021 → 1148; zero ID
leaks; label-free throughout — no Big5/MBTI value was read at any point).
Tier: EXPLORATORY, corpus-level, one Reddit cohort (PANDORA), author
structure only — never psychological validity, never person claims.

## The question the line answered

The T-line closed the Where × Who crossing on static bags. The 4W theory's
§2.4 names the unposed quadrant: When — order, path, state, drift — whose
general object is the author transition kernel K_u (eq 12), every fixed
statistic being only a projection. The U-line measured the first projections
on both time scales.

## The arc

| leg | question | verdict | headline |
|---|---|---|---|
| U1 | does event ORDER carry author structure beyond the bag? | `ORDER_CHANNEL` | rho 0.2893 [0.2695, 0.3114]; dwell-dominated (one stay-rate scalar = 62.3% of the channel); reply-chain mechanics 43.8%; order reproducible but REDUNDANT vs the bag at this resolution (real 0.9571 < bag 0.9607) |
| U2 | is the signature a standing wave or a moving one? | `DRIFT_WITH_CORE` | E(0–90d) 0.6573 → E(2–3y) 0.3515; D 0.3058; THREE-YEAR core 53% (no flattening at the endpoint — permanent-floor language forbidden); the lean missed over: AUC over-predicts persistence |
| U2b | WHICH layer moves (derived T-line prediction)? | `POOL_GATE_UNMET` | the #69 gate held (348 authors < 400); quarantined provisionals: sign unanimous AGAINST the derived prediction (common slower); L2 held (taste floor share 0.6909 highest) |
| U2c | the completion: decay-rate contrast, disclosure-first | `SIGN_UNRESOLVED` | Λ = +0.0741/y [−0.0558, +0.1854]; 7/7 configurations point-positive (distinct faster); no interval excludes zero; λ_taste smallest in BOTH arms (0.1736/0.1515) |

## What the When quadrant now says

1. **Fast time: the order channel is inertia.** Selection order carries
   reproducible author structure (the first positive projection of eq 12),
   and it is dwell — how long one stays — not fine transition structure:
   71.2% of adjacencies are same-state, one scalar recovers 62% of the
   channel. It is the theory's crossing-#4 candidate "inertia," measured
   label-free and kept technical (§5.4). But order is REDUNDANT with the
   bag for absolute identity at 25-state resolution: reproducible ≠
   additive.

2. **Slow time: the person is a moving distribution with a three-year
   core.** The personal excess over an epoch-matched cross baseline loses
   ~46% in three years and has not flattened by the corpus horizon. The
   core is what survives three years — a permanent floor is undemonstrated.

3. **The layer structure in time (outer comparison closed, middle open).**
   The low-rank taste direction is the slowest object in the line
   (λ 0.17/y vs full 0.29/y, smallest in both arms) — the same coordinate
   that carries 82% of the identity budget (T3) is also the most durable.
   Whether the common mass outlasts the distinctive tail is OPEN: every
   point estimate says yes (7/7), no registered interval excludes zero;
   resolution belongs to the second corpus, not to same-corpus escalation.

4. **The methodological law the line pays forward:**
   **discrimination scores over-predict geometric persistence.** AUC 0.9837
   and a 53% three-year floor share coexist because the cross floor is
   0.05. Re-identification survives motion not because people stand still
   but because others are far away. Stability may never be inferred from a
   discrimination score — the slow-time mirror of U1's "reproducible but
   redundant."

## The line's proposition (provisional middle pending the second corpus)

**The person is a moving distribution. What stands longest is the low-rank
taste direction; identification survives motion because others are far
away.** If the open middle closes as every point estimate leans, the T-line
proposition acquires its When clause: the distinctive is the ink, the
taste coordinate is the hand — identity's cross-sectional carrier churns
fastest while the direction it expresses stands.

## Method purchases (defects #76–#80, all planner-owned, all conventioned)

- #76 synthetic worlds register OPERATING POINTS, not just mechanisms
  (U1's saturated first world; A1 stop fired correctly).
- #77 census statistics pin their exact computations (three instances
  across U1/U2/U2b).
- #78 gate quantities are censused by their exact predicate — never derived
  from marginals (U2b's intersection miss, #77's first instance with
  teeth); pool gates stop at the verdict with quarantined provisionals,
  instrument gates stop before real data.
- #79 equivalence cells carry registration-time width projections; ratio
  estimands get log/slope contrasts (U2b's unreachable band and heavy-tailed
  ratio null).
- #80 transformed-scale nulls verify the transform's domain UNDER the null
  (U2c's log-slope null, 7/499 defined); width projections model per-unit
  noise, not pool counts alone (the m=5 paradox: bigger pool, wider
  interval).
- Executor contributions adopted: RD-U2-1 (exact stratified cross baseline,
  certified against the registered sampler); RD-U2B-1 (stop-at-verdict for
  pool gates); RD-U2C-1 (domain-safe null companion); U1's A-U1-1
  recalibration under a declared grid.

## Registered-next and named-open

- **U3 (registered-next, not opened):** the Who × When trait join — the
  cross-thread stay rate (U1's binding designation) and slow-time
  coordinates against Big5 distances, ONLY inside a config-before-joint
  stamped stage-E design (S-line machinery), with the drift-aware caution
  that slow-time coordinates are moving targets.
- Named-open with prices: distinct-vs-common closure (second corpus);
  long-horizon floor (corpus-bounded at ~4y); the non-dwell order residual
  (37.7% of U1's channel); finer-state order (invisible below 25 states by
  construction).

## Commit ledger

| commit | what |
|---|---|
| a0cb613 | U-line opened; U1 registered before run |
| eb5bcf2 | U1 leg — ORDER_CHANNEL |
| 9313b27 | U1 adjudication; defect #76 |
| b864dc7 | U2 registered before run |
| 17ca68a | U2 leg — DRIFT_WITH_CORE |
| 2c4b748 | U2 adjudication; defect #77; U2b chartered |
| 96f7e40 | U2b registered before run |
| eca6d37 | U2b leg — POOL_GATE_UNMET |
| 6a4a5bb | U2b adjudication; defects #78/#79; U2c registered before run |
| d6ead28 | U2c leg — SIGN_UNRESOLVED |
| (this) | U2c adjudication; defect #80; phase close; this synthesis |
