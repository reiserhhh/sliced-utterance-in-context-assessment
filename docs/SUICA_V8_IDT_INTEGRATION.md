# V8 × IDT — The Integration Document

Status: **INTEGRATION OF RECORD, 2026-08-10.** This document folds the
identity-era results (the M4-K identity line, the M4-L typology line, the
defense phase D1–D5 + D-open, and M4-K2f) into the V8 unified theory
system. It is a MAP, not a re-derivation: every claim cites its
controlling document; the claims ledger controls; nothing here upgrades a
tier. Companions: `docs/SUICA_UNIFIED_THEORY_SYSTEM_V8.md` (the system),
`docs/SUICA_IDENTITY_THEORY_V1.md` (IDT, appendices A–V),
`docs/SUICA_M4_K_IDENTITY_LINE_PLAN.md` / `SUICA_M4_L_TYPOLOGY_LINE_PLAN.md`
/ `SUICA_DEFENSE_PHASE_PLAN.md` (every registration, outcome,
adjudication), the three line/phase syntheses, and
`docs/SUICA_IDT_総括_JA_20260809.md` (Japanese overview of the first arc).

Tier of everything below: EXPLORATORY, synthetic, instrument-world laws.
Nothing licenses claims about corpora, constructs, persons, or diagnoses.

---

## 1. What IDT adds to V8's measurement chain

V8's chain runs human process → record → representation → technical
object → typed reference-relative measurement (F01..F05), with refusal
gates at each link. IDT adds a **frame calculus** to the middle of the
chain and a **reader theory** to its last link:

- **The frame F = (P, O, h, U)** — issuer (reference sample),
  jurisdiction (occasion universe), expiry (horizon), units
  (representation). Every deviation-based object is typed by its frame;
  T1 (gauge trichotomy) proves magnitude is three-ways gauge-dependent
  while T2's invariant layer (centered configuration, shares, angles
  under scalar gauge) is the citable stratum. This SHARPENS V8's F03/F09
  refusals: "no universal coordinate" now has a theorem-shaped reason.
- **The reader is part of the measurement chain.** The deployed
  relation-field gauge is not a neutral window: it AMPLIFIES
  common-frame content (K1: +0.0925 at a 1× shift = 3.54× the F2
  composition effect), loses trait content like the SQUARE of the card's
  attenuation, and taxes raw person-state variance (κ ≈ 0.72, three
  independent fitting routes). V8's instrument-first stance is thereby
  mechanized: **the F5 plateau is a property of the reader, not of the
  information.**

## 2. Corrections IDT forced on V8-era conclusions (all by dated note)

| V8-era claim | corrected reading | where |
|---|---|---|
| M4-F2 "composition works at fixed budget" | the gain is COMMON-FRAME content at every tested knob; author content is interference (author share −0.949 / −0.944) | F-synthesis dated notes I–II |
| M4-F4 "author axis, γ = 1.096" | a REPLICATE axis — survives total author deletion, steeper and ~half the budget without authors | F-synthesis note III; K1d |
| M4-F5 "truth recovery" | recovery of a MIXTURE (½ author + ½ common at κ=1.0); plateau re-attributed to the reader | note III; K1d G-info-F5; K2b–K2e |
| "de-framing repairs the gauge" (candidate) | de-framing is DIAGNOSTIC-ONLY: it deletes the trait's scaffold (b-only recovery → 0 in 6/6 arms) | K-R1; IDT appendix N |
| T4 composite as a LEVEL formula | withdrawn (missed a sealed band by 5.09 widths); restored as field ≈ λ′ − κ′·V_person, interpolation-grade | D-open; K2f; appendices U–V |

One root cause is on the record: M4-F7's coefficient-0 finding was never
propagated to F2/F4/F5's attributions. The retrospective mechanism
(dated notes, never rewrites) carried all three corrections.

## 3. New laws, graded (the grade IS the claim)

- **PREDICTIVE (sealed, then hit at unseen configurations):**
  the η-floor law σ_u²(η) = η·σ_b²/k_τ + (1−η)·σ_b²/m (3/3 at m=96,
  k_τ=4, G=5); the taxometer η̂ (±0.125 certificate; 0.0042 error at a
  novel cell); the tax-ratio algebra √(m/k_τ) (8.9e-16 + strict
  ordering).
- **RESPONSE-GRADE (laws of differences/swings, verified out-of-sample
  as such):** the variance tax D ≈ −0.722·ΔV_person (R² 0.9935;
  out-of-sample error 0.0019); the quadratic over-response (q as a BAND
  1.83–1.93, per the D2 fragility annex).
- **LEVEL-GRADE, interpolative:** field ≈ λ′ − κ′·V_person (predicted a
  fresh arm at 25.3% of its band; exponent unidentifiable at level on
  this corpus — designed collinearity −0.964; extrapolation unsealed).
- **DEAD, by measurement:** the (d/n)^¼ window-width conjecture
  (sealed OPEN, measured EMPTY); T8(e) angular crowding (readability
  drives misidentification in sparse galleries, AUC 0.664 vs chance);
  the T4 level composite v1; strict function-of-attenuation.

## 4. New principles at V8 level

- **T9, the forgery principle:** frame content forges identity-like
  statistics at the individual level (issuer error → person-stable fake
  that PASSES plain reproducibility, +0.055/+0.059 at both knobs) and
  the collective level (shared occasions → fake agreement). Licensed
  counter-operations: frame REFRESHMENT and frame CONTRAST — as
  diagnostics only (the scaffold corollary: in families where person
  content has no frame-free expression, frame REMOVAL destroys forgery
  and legitimate reading together).
- **T10, anchor impossibility (proved):** no informative identity
  reading is invariant to issuer, gallery, and coordinates
  simultaneously; the three anchor prices are exhaustive and all three
  are measured. V8's reference-relative spine is thereby not a design
  choice but a necessity.
- **The complementarity principle:** frame / type / identity / state /
  noise — each layer is signal for exactly one question and
  interference for the others; a reader must declare its target layer,
  and each layer's valid suppression operation is structural (averaging
  for noise, refreshment for state and issuer forgeries, cross-person
  pooling for type; identity has NONE without labels — which is R1's
  circularity and the typology line's whole subject).

## 5. New instruments in the V8 toolbox (with certificates)

| instrument | what it reads | certificate |
|---|---|---|
| frame-refreshed discriminator (T6″ v2) | identity vs forged identity | forgery destroyed and inverted at both knobs; do-no-harm ≤ 0.003 |
| two-split probe (T6′) | state share / τ_s at card level | 11/12 + exact ordering vs algebra (K2a) |
| taxometer η̂ | which geometry a world is in (types vs dimensions) | ±0.125, 10/10; +O(0.05) pole bias; whitening-shape precondition |
| completeness meter | a typology's surviving-identity share ("error = 0" as an audit) | certified on true/given partitions; propagation-corrected; noise floor ~0.005 |
| card-family readers | trait questions | ~4.5× the relational gauge at trait reading; the gauge stays the occasion-bound-object instrument |

## 6. Foundation-gap ledger interface

- **F10 (state/trait):** unchanged in status, but its per-construct
  empirical side now has a MECHANISM: the within-horizon
  non-identifiability of b vs s̄ (T4 typing), the reader's quadratic
  loss + variance tax, and the two-split probe as the licensed state
  meter. The refusal ("no trait claim from same-session split-half")
  gains T9's sharper form: same-frame reproducibility is FORGEABLE.
- **F08 (residual semantics):** reinforced twice — S4's
  non-reproducibility (H6) and the identity discriminator's rule that
  only frame-refreshed-stable components may be named.
- **PROPOSED new rows** (owner's call under the closure standard, not
  added unilaterally): F12 frame-forgeability (T9) and F13
  anchor-dependence (T10) as constitutional entries.

## 7. Governance and defense state

`docs/SUICA_REALTEXT_GOVERNANCE.md` (R-G1..R-G8) binds all future
real-text work: no person claims; T6″ required; NO cross-corpus author
linkage ever; de-framing diagnostic-only; typology completeness
disclosure; salt-embedded seals; one exception path. Defense record:
adversarial verification of the 10-row headline table (0 refuted);
portable lockbox (17 trees, committed manifest); prospective seal opened
measure-first (3/5 predicted; both misses converted into named
knowledge). Standing rules now 1–24; defect registry #1–#42
(`docs/SUICA_DEFECT_REGISTRY.md`); under today's rules the replay puts
ZERO defects at post-hoc catch stage.

## 8. Frozen operators — unchanged, with two certified-unadopted repairs

`colstd_alpha_0.10` (M4-G6) and estimated de-framing (K1b/K1c′; with the
K-R1 caution: composition-deflation yes, trait reading NO). Adoption =
new operator under F16 = the owner's decision. Nothing in the IDT era
touched a frozen operator.

## 9. Open, named, not queued

The r-at-level question (needs a decollinearized corpus); an
extrapolation-grade seal for the level law; dense/heterogeneous-gallery
crowding; the Bayes shoulder's dimension-dependence; real-text
transport (still a limitation, not a queued test — no shared-occasion
typed corpus exists); native corpus (paused by the owner, 2026-07-12).

---

## Dated addendum (2026-08-11) — the M4-M level-law completion line

The M-line (7 legs, `140e927`..`1b0d330` + close) updates §2 and §3;
synthesis: `docs/SUICA_M4_M_LEVEL_LAW_LINE_SYNTHESIS.md`.

- **§3 graded laws — additions.** New grade **PREDICTIVE-SCOPED**:
  the scoped level law (free share-margins + steep negative
  r-channel; predictions with realized r interior to
  [0.454, 0.819]) — sealed-then-hit 3/3 at share-exterior,
  φ-exterior, interior-new-φ (M2; zero pre-stamp generations).
  LEVEL-grade, interpolative, closure-validated: the tax curve
  α(V) = c − κ0V + (κ2/2)V² on V ∈ [0.03, 0.21] (κ0 0.960 [0.894,
  1.031], κ2 1.563 [1.032, 2.120]); local tax declines 0.913 →
  0.632; retrodicts 5/6 published κ̂ through each estimator's own
  pipeline. DEAD, by measurement: the constant tax; "q = −0.150" as
  a structural exponent; additive separability of the level field;
  the naive φ-power representation; M1's marginal-correlation
  feasibility gate.
- **§2 corrections — one row added.** Appendix V.2's "the quadratic
  over-response remains a RESPONSE-structure fact" → re-attributed:
  the response-grade band was the variance tax through collinear
  share-sweeps; no V-clean positive-exponent measurement exists;
  the V-omitted shadow fit on the decollinearized corpus gives q =
  +2.245 [2.177, 2.319] (appendices X.2, Y.2; yield clause stands).
- **§7 governance state — counters.** Standing rules now 1–29
  (25 estimand-gates; 26 bound-winners; 27 consumption budgets;
  28 representation licensing; 29 domain predicates); defect
  registry #1–#49; M3 closed with zero registration defects.
- Nothing here touches frozen operators, F16 items, PROPOSED
  F12/F13, or any real-text question.

## Dated addendum (2026-08-14) — the M4-N tax-mechanism line

The N-line (4 legs, `76060e7`..close; synthesis
`docs/SUICA_M4_N_TAX_MECHANISM_LINE_SYNTHESIS.md`) updates §3:

- **The tax curve is promoted to PREDICTIVE (response-transport),
  V ∈ [0.03, 0.21]:** fitted at level, sealed-then-hit 3/3 in
  response space (κ̂_HI to 8.3e-5; the decline's CI excludes zero).
  The constant tax is DEAD in both spaces by sealed measurement.
- **The six-target closure reads 5 hits / 1 within-noise / 0
  discrepant** (rule 32's origin case: the one "miss" sat at 1.37 SE
  of its own estimator against a 1.19-SE-wide tolerance).
- **Mechanism state:** square-agreement (appendix CC.1′) is a
  candidate with one sealed transport hit; its remaining questions
  are unposable on this generator (N2 dissolved; N3 priced at
  executed arithmetic — twins to z < 0.1 across the fitted window).
- Standing rules now 1–32; defect registry #1–#54. Successor:
  the M4-P penalty-mechanism line.

## Dated addendum (2026-08-14, second) — the M4-P penalty-mechanism line

The P-line (5 legs, `7589299`..close; synthesis
`docs/SUICA_M4_P_PENALTY_MECHANISM_LINE_SYNTHESIS.md`) adds the
mechanism layer the M/N laws lacked:

- **The gauge is a frame-agreement instrument** (appendix II):
  injected frame moves the b-only statistic 96–97% through
  frame-vs-frame agreement (f = 0.958/0.971); the natural
  φ-gradient is ~88% frame-owned; cross-frame levels are near zero.
  Genuine cross-frame person transport ≤ ~1/3 (CI), point ~1/9.
  The M/N laws stand as laws of the statistic; their mechanism
  sections re-type.
- **T9's two refreshment layers** (appendix HH): reader-side
  (existed, T6″) vs generator-side (impossible on the published
  interface — instrument theorem; now EXISTS as the certified
  split-seed extraction, a standing asset with a live anchor).
- Standing rules 1–32; defects #1–#58 (#55–#58 bought conventions:
  NULL-first classifications, inheritance-is-not-exemption, pilot
  second moments, sub-case carry-forward).
- Successor: the M4-Q transport line (cards under refreshment —
  the instrument-role taxonomy's mechanism half).
