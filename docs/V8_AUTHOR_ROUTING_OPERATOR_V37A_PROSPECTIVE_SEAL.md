# V8 Author Routing Operator V3.7A Prospective Seal

Sealed: 2026-07-26, before canonical seed `20310429` was run.

This is an internal content-hash seal, not an externally timestamped
preregistration. The repository worktree is dirty, so the machine-readable
hashes in `configs/v8_author_routing_operator_v37a_seal.json` are the operative
freeze boundary.

The pre-seal power run passed 21/21 gates using excluded construction seeds.
The canonical run must verify every sealed hash before simulation begins.
Changing a config, gate, estimator, runner, or test after reading canonical
results invalidates confirmatory status and requires a new version and seed.

The seal licenses no personality, intelligence, clinical, causal, real-text,
or arbitrary-full-rank claim.
