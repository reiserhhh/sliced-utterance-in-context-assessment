# SUICA V8 Mathematical Research Route and Evidence Archive

Status:
`EVENT_TO_RELATION_SEAM1B_SUPPORTED_SYNTHETIC__M3_V4_READY_FOR_CLEAN_SEAL__K_CMOSO_BACKGROUND_ONLY__M_FILTERED_CONCORDANCE_ALL_THREE__FILTERED_GLOBAL_AXES_NONE__PANDORA_SHORT_SCALE_AND_X_MULTI_SCALE_CONTEXT_STRATIFIED_RELATION_CORRESPONDENCE__CONTEXT_TRANSPORT_UNDERRESOLVED__PSYCHOLOGICAL_MAPPING_CLOSED`

Date: 2026-07-30

This document is the authoritative route map for the mathematical research
developed after the sealed `v0.2.1` release. It integrates the V8 assessment,
conditional-response, geometry, reliability, limit, residual-completion, and
M3 micro-to-meso lines into one framework.

It is not a new release seal. It does not reopen the `v0.2.1` lockbox, promote
development results to confirmation, or claim that an anonymous mathematical
object is personality.

All new studies on this route are now subordinate to
`docs/SUICA_FOUNDATIONAL_CLOSURE_STANDARD.md` and the executable
`suica.foundation.study.v1` contract. This does not turn SUICA into a scale.
It prevents a local technical seam from being silently promoted into a
measurement, psychological construct, or use claim.

## 1. Scientific target

SUICA is a general methodology foundation for constructing and testing
natural-language measurements. It is not itself a personality scale, symptom
scale, diagnostic system, or clinical instrument.

The V8 target is to identify and estimate reproducible author-relative
structure in text while preserving four separations:

1. observed text events versus latent author process;
2. author process versus measurement error;
3. technical author structure versus psychological interpretation;
4. deterministic measurement versus LLM explanation.

The numerical channel remains deterministic. A frozen LLM may convert
registered evidence into constrained human-readable explanations, but it may
not invent scores, constructs, diagnoses, validity, or certainty.

## 2. Unified mathematical object

For author \(u\), opportunity \(o\), condition \(c\), occasion \(t\), and
observer \(r\), the V8 mesoscopic observation model is

\[
c_{ui}\sim\pi_u(c\mid o_{ui}),
\]

\[
y_{uictr}
=
\mu_{c,o}+a_u+
\left[B_0+F(\eta_u)+\varepsilon D_u\right]z_c
+Hs_{ut}+\delta_r+e_{uictr}.
\]

Under the foundation ontology, its anonymous L4 technical author object is

\[
\mathfrak A_u
=
\left(
a_u,\pi_u,[B_u],R_u^{\mathrm{stable}},
U_u,E_u;\mathcal P_{\mathrm{ref}}
\right).
\]

The M3 line supplies the microscopic process beneath that object:

\[
\mathcal N_u
=
\sum_{o,t}
\delta_{(o,t,O_{uot},C_{uot},S_{uo},\Xi_{uot})}.
\]

The integrated route is therefore

\[
\mathcal N_u
\xrightarrow{\mathcal C}
\mathfrak A_u
\xrightarrow{\mathcal E_m}
\widehat{\mathfrak A}_u^{(m)}
\xrightarrow{E45}
\mathfrak M_u
\xrightarrow{\Psi}
\mathcal Y_u.
\]

The anonymous mesoscopic author object is

\[
\mathfrak A_u
=
\left(
\pi_u,G_u,a_u,[B_u],h_u,\mathcal K_u,\mathcal C_u,\mathcal P_u
\right).
\]

Here:

- \(\mathcal C\) is a family of coarse-graining maps from event paths to
  mesoscopic author objects;
- \(G_u\) is the condition-indexed author-relative response field;
- \(a_u\) and \([B_u]\) are its stable position and response-operator
  equivalence class;
- \(h_u\) is unresolved nonlinear structure outside the registered response
  family, not automatically noise;
- \(\mathcal K_u\) is a temporal or transition operator;
- \(\mathcal C_u\) is an interactional coupling object;
- \(\mathcal P_u\) is higher-order path structure;
- \(\mathcal E_m\) is a finite-resolution estimator with uncertainty and
  refusal;
- \(\Psi\) is the external construct mapping.

The symbol \(\Psi\) remains closed. V8 currently studies the existence,
identification, estimation, geometry, and reliability of anonymous technical
objects. Psychological naming requires external evidence designed for that
construct.

## 3. Why M3 belongs inside V8

M3 is not a separate product line. It is V8's micro-to-meso existence and
identification layer.

Earlier V8 work asked whether author-relative positions, response operators,
incidence structures, routing kernels, and residual channels can be measured
under declared designs. M3 asks the complementary question:

> Can different microscopic text-path laws generate stable author structure
> that ordinary means, low-order moments, or short-lag covariance cannot
> identify, while a fixed generator-blind measurement battery recovers it and
> refuses exact aliases?

M3-V4 operationalizes three anonymous object families:

- distribution geometry beyond degree-four moments;
- nonlinear response operators beyond a joint polynomial nuisance space;
- path dynamics beyond low-order covariance, including dwell hazard,
  probability current, and nonlinear variance dynamics.

These are candidate mechanisms for the middle of the V8 map. They are not
named factors and are not evidence that any recovered coordinate is a
personality trait.

## 4. Research route

### Route A: evidence-carrying assessment

V8.0-V8.4 established the executable contract, provenance chain, explicit
refusal rules, explanation certificates, and the role boundary between
deterministic measurement and LLM rendering.

The LLM semantic channel recovered planted events accurately but failed the
registered stability/increment gates on real text. The accepted architecture
therefore keeps the LLM as an evidence renderer rather than a numeric scorer.

Primary records:

- `docs/V8_LLM_NATIVE_ASSESSMENT_THEORY.md`
- `docs/V8_EXECUTION_PLAN.md`
- `reports/V8_FULL_TECHNICAL_EXPERIMENT_REPORT.md`
- `reports/V8_FULL_PROVENANCE_AUDIT.md`

### Route B: conditional response and author geometry

The canonical response geometry separates:

- C1 selected exposure, including entered, avoided, revisited, or created
  opportunities;
- C2 response under shared conditions;
- stable author position;
- occasion state, observer variation, and finite-text error;
- population, group, and within-group individual structure.

Canonical geometry improved source-disjoint author discrimination within
PANDORA, but the first explicit behavior bridge did not support a stable
cross-modal link. The geometry and behavior channels must therefore remain
separate until a registered bridge succeeds.

An exploratory full-PANDORA external connection was subsequently run with the
frozen native V8 operator before questionnaire labels were joined. The
invariant canonical score did not predict Big Five (official-fold mean
\(r=-.005\)) and was weak for MBTI (mean AUC \(=.544\)), but it recovered the
held-out \(5\times4\) Big5-to-MBTI relation matrix
(\(r_{\mathrm{element}}=.498\), permutation \(p=.0086\), author-bootstrap
95% CI [.383, .582]). The structural gain over a text-volume/format nuisance
view was .289 [95% CI .125, .430]. This is evidence that frozen text geometry
carries cross-scale relational structure, not evidence that any coordinate
is a personality construct. The labels had already been opened, the MBTI
representation is label-free but unsupervised-transductive, and direct
prediction remains materially stronger in the upstream 48-coordinate
representation.

A hierarchical follow-up then kept the upstream 48-coordinate author object,
canonical 16-coordinate relational contraction, and 9-coordinate
opportunity/observation surface as separate evidence levels. Source-only
nested official folds selected task-local read routes before any bridge values
were loaded. The hard router was noninferior to upstream for MBTI
(AUC .608 versus .607; delta .0007, 95% CI [-.0014, .0026]) but not for Big
Five (mean \(r=.049\) versus .062; delta -.013, 95% CI [-.040, .018]).
Its held-out bridge relation remained nonzero
(\(r_{\mathrm{element}}=.463\), permutation \(p=.0174\)); a joint
source-head/bridge-author bootstrap gave 95% CI [.140, .621]. It did not
outperform upstream on bridge structure (delta -.061, 95% refit-bootstrap CI
[-.218, .112]). Big Five route choices were notably unstable across folds,
especially Neuroticism (four routes; modal share .40). Fixed all-layer and
upstream-plus-canonical reads reached descriptive bridge
\(r_{\mathrm{element}}=.546\), but these opened-label comparisons cannot be
promoted post hoc. The result supports a layered evidence architecture and
rejects a universal-vector interpretation; it does not yet validate hard
winner-take-all routing.

Primary records:

- `docs/V8_CONDITIONAL_RESPONSE_GEOMETRY_THEORY.md`
- `docs/V8_HIERARCHICAL_EVIDENCE_STACK_THEORY.md`
- `reports/V8_CANONICAL_ORBIT_GEOMETRY_REPORT.md`
- `reports/V8_CANONICAL_BEHAVIOR_BRIDGE_REPORT.md`
- `reports/V8_BEHAVIOR_C2_PLANTED_WORLD_REPORT.md`
- `reports/V8_PANDORA_EXTERNAL_CONNECTION_REPORT.md`
- `reports/V8_HIERARCHICAL_EVIDENCE_STACK_PANDORA.md`

### Route B2: typed identifiability and relational licenses

The hierarchy is not a single text-to-questionnaire chain. The latent
technical author object, its measurement estimate, uncertainty/refusal,
population text geometry, and external calibrated readouts are different
types. HJIC-1 tested that graph in synthetic worlds. Eight of nine gates
passed; the shared relational-lift gate failed because the original protocol
incorrectly required almost every planted shared-latent world to issue a
finite-sample license.

HJIC-1A replaced that implication with a post-license fidelity target and
separated two claims:

\[
\delta_{\mathrm{relation}}
\quad\text{and}\quad
\delta_{\mathrm{unique\ mode}}.
\]

Its replicated-support estimator correctly licensed stable relation matrices
without forcing a unique mode under a low singular gap. It nevertheless
remained partial: `SIMPSON_MIXTURE` received a false relation license in
8/60 repetitions.

HJIC-1B preserved both partial predecessors and added a
`NUISANCE_INVARIANCE_VETO`. It compares raw and cross-fit-conditioned
relations using a symmetric Frobenius shift and maximum cell shift, requires
a 99% author-bootstrap material lower bound and row-wise nuisance-permutation
\(p\le.01\), and never reads synthetic truth when licensing. In 60 prospective
repetitions all 10 gates passed:

- shared relation/mode license = .983/.800;
- low-singular-gap relation/mode license = 1.000/.000;
- high-dimensional irrelevant nuisance relation license = 1.000 with zero
  false veto;
- global plus localized Simpson detection = 1.000 with zero final license;
- collider/descendant conditioning sensitivity detection = 1.000 with zero
  final license;
- weak relation license = .000 and nuisance-veto rate = .017;
- common nuisance, correlated replicate error, and private axes each had zero
  relation licenses;
- post-license truth fidelity = 1.000 across 119 licensed positive worlds,
  while truth usage by licensing was zero.

This closes the registered global condition-sensitivity hole. It does not
show that conditioning recovers causal truth or that the conditioned variable
is noise.

HJIC-1C then implemented the context-indexed field
\(\mathcal J=\{(p_s,J_s)\}\) and the covariance-first macro object
\((J_W,J_B,J_T,H,\kappa)\). It applies one calibration-frozen whitening map
only after the total-covariance decomposition, so
\(J_T=J_W+J_B\) is an auditable identity rather than an analogy. The formal
60-repetition run used 999 null permutations and 999 author bootstraps and
passed all 13 gates:

- covariance-decomposition q95 error \(=1.94\times10^{-15}\);
- pooled nominal 95% cell coverage \(=.943\);
- global-invariant license \(=1.000\), heterogeneity false alarm \(=.000\);
- sign-reversal local-atlas/cancellation/global rates
  \(=1.000/1.000/.000\);
- true-moderation local-atlas \(=.983\), false global license \(=.017\);
- ecological-only detection \(=1.000\), false individual license \(=.000\);
- composition attribution \(=.983\);
- out-of-sieve misspecification detection \(=1.000\), final false relation
  \(=.000\);
- low-gap relation/mode license \(=1.000/.000\);
- high-reliability recovery and low-reliability refusal \(=1.000/1.000\);
- truth usage by licensing \(=.000\).

This licenses a synthetic replicated-mesoscopic-to-macro relation-field
procedure.

V8-M3-HJIC-SEAM-1 then tested the previously open raw-event arrow:

\[
\mathcal N_{u,s}^{f,r}
\to \widehat A_{u,s}^{f,r}
\to (\widehat J_s,\widehat J_W,\widehat J_B,\widehat J_T,H,\kappa)
\to \text{license/refusal}.
\]

The first formal run correctly preserved 9/10 gate families but remained
partial because ecological detection was .850. It reused the family-wise
max-context local threshold for the single between-context matrix. SEAM-1A
gave \(J_B\) its own D0 permutation null. Ecological detection rose to .967
with zero individual or held-out-null false licenses, isolating the
statistic-specific threshold defect. SEAM-1A remained partial because the
original generator also changed its latent-to-event support between D0 and
confirmation.

SEAM-1B turned that second hidden assumption into an observable gate. For raw
replicated M3 feature supports \(Q_{f,0}\) and \(\widehat Q_{f,d}\), it uses

\[
A_{f,d}=\sigma_{\min}^2(Q_{f,0}^{\top}\widehat Q_{f,d})
\]

after separately checking the positive second eigenvalue and its eigengap.
The independent 60-repetition, 999-resample formal battery passed all 13 gate
families:

- clean end-to-end seam license = 1.000;
- sample-latent relation fidelity q05 = .992 and relative-error q95 = .174;
- analytic-population 95% coverage = .924;
- covariance decomposition q95 error = \(1.33\times10^{-15}\);
- attenuation correction win rate = .967 with median error reduction .569;
- cancellation and ecological detection = 1.000 with zero false global or
  individual licenses;
- global and ecological support-drift detection = 1.000 with zero final seam
  licenses;
- support-underresolution detection = 1.000 with zero final seam licenses;
- within-support-gauge false refusal = .017;
- observable truth usage = .000.

The identified object is an observable event-to-relation procedure plus a
support-comparability license. It does not identify the unique hidden
measurement operator, construct a transport map after support drift, or
establish personality meaning.

Primary records:

- `docs/V8_HIERARCHICAL_IDENTIFIABILITY_PROTOCOL.md`
- `reports/V8_HIERARCHICAL_IDENTIFIABILITY_REPORT.md`
- `docs/V8_RELATIONAL_LIFT_SUPPORT_PROTOCOL.md`
- `reports/V8_RELATIONAL_LIFT_SUPPORT_REPORT.md`
- `docs/V8_NUISANCE_INVARIANCE_VETO_PROTOCOL.md`
- `reports/V8_NUISANCE_INVARIANCE_VETO_REPORT.md`
- `docs/V8_CONTEXT_FIBERED_RELATION_PROTOCOL.md`
- `reports/V8_CONTEXT_RELATION_FIELD_REPORT.md`
- `docs/V8_M3_HJIC_SEAM_PROTOCOL.md`
- `reports/V8_M3_HJIC_SEAM_REPORT.md`
- `docs/V8_M3_HJIC_SEAM_1A_PROTOCOL.md`
- `reports/V8_M3_HJIC_SEAM_1A_REPORT.md`
- `docs/V8_M3_HJIC_SEAM_1B_PROTOCOL.md`
- `reports/V8_M3_HJIC_SEAM_1B_REPORT.md`
- `docs/V8_MICRO_MESO_MACRO_SEAM_CLOSURE.md`

### Route C: sets, topology, and path dynamics

The V3.0-V3.7 sequence progressively replaced point-only descriptions with
condition-indexed response sets and path operators:

1. response-set incidence distinguished same-condition from free-image
   proximity;
2. persistent hyperedges represented simultaneous multi-author feasibility;
3. condition indices were shown to carry information lost by aggregate pair
   matrices;
4. common-ball birth scales distinguished pairwise adjacency from joint
   feasibility;
5. local transversality separated registered contact types;
6. spacetime routing separated junction geometry from transition choice;
7. author-specific routing operators introduced group-conditioned scoring,
   blind localization, transport, adaptive rank, and fixed reference panels.

These results are synthetic technical identification results. Intersections,
routes, clusters, rings, residuals, and dynamic axes are not psychological
constructs without external validation.

### Route D: reliability, resolution, and zero limits

V3.7G-V3.7H.4 asked which apparent residuals are finite-resolution
measurement effects and which survive declared limits.

The core progression is:

\[
\mathcal F_{m_1}\subset\mathcal F_{m_2}\subset\cdots,\qquad
\widehat{\mathfrak A}_u^{(m)}
=
\mathbb E[\mathfrak A_u\mid\mathcal F_m]
\quad\text{only under a justified estimator model}.
\]

The experiments established narrower results:

- V3.7G: a core observable reliability spectrum passed in its registered
  synthetic setting, with an NMSE/NRMSE terminology erratum;
- V3.7H: resolution-filtration discovery completed but was not confirmed;
- V3.7H.1: risk contracted, but the process was not licensed as a posterior
  martingale;
- V3.7H.2: repeated opportunities separated common shock from technical
  replication, subject to a replication boundary;
- V3.7H.3: multiscale zero identification partially passed in an additive
  balanced world;
- V3.7H.4: misspecification tests refuted an overconfident additive
  interpretation.

The accepted lesson is that "more text" and "more authors" can reduce
specific error components, but they do not force every unresolved component
to zero.

### Route E: reference measure and residual completion

V3.7H.4D and R1-R2G.2 tested whether residual structure can be localized,
predicted, or completed under different reference and hierarchy limits.

The sequence is intentionally preserved with its failures:

| Stage | Current ruling | What it changed |
| --- | --- | --- |
| H.4D | `SMOKE_REFUTED_IDENTIFIABILITY` | An absolute post-centering interaction target was algebraically invalid. |
| R1 | `PARTIAL_REFERENCE_CONTRAST` | Reference contrasts were recoverable, minority sensitivity remained unresolved. |
| R2B | `PARTIAL_OPERATOR_PREDICTIVE_NOT_SUFFICIENT` | Geometry predicted detector power but did not fully explain it. |
| R2C | `REFUTED_PERMUTATION_ORBIT_EXPLANATION` | Cell calibration worked; individual conditional prediction did not. |
| R2D | `STOP_NO_EXPLOITABLE_CONDITIONAL_TARGET` | The registered process was primarily cell-level. |
| R2E.2 | `CONFIRMED_NORMAL_ONLY` | One normal direction was active; the registered tangent family was practically null. |
| R2F.1 | `STOP_NO_LOCAL_TANGENT_CANDIDATE` | A valid search found no material stable local candidate. |
| R2G | `INCONCLUSIVE_BOUNDED_COMPLETION` | Four score opportunities were insufficient for practical zero. |
| R2G.1 | `PARTIAL_CONSTRUCTIVE_EXISTENCE` | Known completable structure approached the factor-completeness limit as score resolution increased. |
| R2G.2/A | `SUPPORTED_SYNTHETIC_HIERARCHICAL_FORMULA_WITH_IDENTIFIABILITY_BOUNDARY` | Society, group, and author precision terms were separated under the registered hierarchy. |

This route supports a conditional statement: some residual floors are
finite-information effects and some are structural aliases or omitted
mechanisms. It does not support the universal claim that the true residual is
zero.

### Route F: M3 cross-family confirmation

M3-V1 is retained as a matched-family engineering baseline, not confirmation.
M3-V2 is a basis-blinded near-family discovery with partial nonlinear
generalization. M3-V4 replaces that weak bridge with cross-family planted
objects and one generator-blind estimator battery.

The 12-seed V4 preflight passed all 17 registered development gates:

- CF-D expected AUC 0.636-0.680 and geometry 0.331-0.468;
- CF-O expected AUC 0.99991-0.99997 and geometry 0.977-0.988;
- CF-KP expected AUC 0.758-0.809 and geometry 0.829-0.876;
- aliases remained at AUC 0.4984-0.5008;
- six null families had maximum mean AUC deviation 0.0119;
- target-knockout geometry magnitude was at most 0.0419.

This result licenses a clean pre-randomness seal. It is not formal
confirmation. In particular, the ARCH proper held-out gain is approximately
\(2.27\times10^{-6}\), so the unopened confirmation may legitimately fail.

Primary records:

- `docs/SUICA_M3_MICRO_MESO_MACRO_FOUNDATION.md`
- `docs/SUICA_M3_MECHANISM_ATLAS_THEORY.md`
- `docs/SUICA_M3_CROSS_FAMILY_CONFIRMATION_PROTOCOL_V4.md`
- `reports/SUICA_M3_CROSS_FAMILY_PREFLIGHT_V4.md`

### Route G: real-text replicated-density resolution

The first label-free PANDORA/Essays/X audit tested two anonymous technical
families:

- \(M\): order-free marginal geometry;
- \(K^\circ\): transition geometry after random-order expectation
  subtraction.

All observed families showed reproducible high-dimensional anisotropic
density, with effective ranks roughly 29--44, while hard axes and a stable
local \(M\leftrightarrow K^\circ\) relation were not identified. The initial
tau-indexed transport pattern was therefore not accepted as containment.

The formal replacement uses the capacity-sharpness set

\[
\mathcal P(c,q)=
\{P:0\preceq P\preceq I,\ \operatorname{tr}P=cd,\
\|P-cI\|_F^2\le qcd(1-c)\}
\]

and its support function

\[
\Gamma_\rho(c,q)=
\max_{P\in\mathcal P(c,q)}
\langle P-cI,\rho\rangle.
\]

The complete \(k=1,\ldots,d-1\) path was tested with simultaneous author
bootstrap and maximum-Haar controls. PANDORA and Essays confirmed
corpus-local resolution regions for both M and K. Confirmed cells were
671/835 and 464/755 for PANDORA M/K, and 700/835 and 393/755 for Essays M/K.
All four D0 internal Haar tests had \(p=.01\); effective ranks were about
35--41. X was correctly refused because D1/D2 had 46/42 authors, below the
frozen 48-author confirmation floor.

At \(q=1\), \(\Gamma\) equals the spectral Lorenz excess:

\[
\Gamma_\rho(k/d,1)
=
\sum_{i=1}^{k}\lambda_i(\rho)-k/d.
\]

This made a second distinction necessary. A corpus-local resolution
signature is orthogonally invariant inside its declared gauge, but it is not
a cross-corpus invariant under separately fitted gauges. The post-hoc
shared-gauge replay used equal D0/D1/D2 author counts of 420/296/269.
Neither observed directional spectral-order hypothesis was supported.
For M, the registered Essays-minus-PANDORA curve was non-positive throughout
D1 and had no confirmed positive region in D2. For K, the
PANDORA-minus-Essays point curve was often positive, but its simultaneous
band never cleared zero. The earlier mask nesting is therefore classified as
`GAUGE_DEPENDENT_THRESHOLD_NESTING`, not majorization.

The accepted type chain is now:

\[
\text{replicated anisotropic density}
\not\Rightarrow
\text{capacity compressibility}
\not\Rightarrow
\text{cross-corpus spectral order}
\not\Rightarrow
\text{common orientation}
\not\Rightarrow
\text{sequence-sensitive phase}
\not\Rightarrow
\text{psychological construct}.
\]

### 4.7 Permutation-orbit correction of the K family

The original K construction mixed two different objects: the variation that
an unordered event multiset permits under reordering, and the position of the
observed order inside that permutation orbit. For a four-event multiset
\(\mathcal M_{ur}\), all \(4!=24\) paths are now enumerated:

\[
\mu_{ur}^{K}=E_{\pi\in S_4}K(\pi\mathcal M_{ur}),
\qquad
V_{ur}^{K}=\operatorname{Cov}_{\pi\in S_4}
[K(\pi\mathcal M_{ur})],
\]

\[
z_{ur}^{K}
=
K(\pi_{ur}^{obs}\mathcal M_{ur})-\mu_{ur}^{K},
\qquad
q_{ur}^{K}
=
(V_{ur}^{K}+\alpha\bar\lambda_{ur}I)^{-1/2}z_{ur}^{K},
\quad \alpha=.10.
\]

The complete technical object is therefore

\[
\mathfrak O_{ur}^{K}
=
(\mathcal M_{ur},\mu_{ur}^{K},V_{ur}^{K},z_{ur}^{K},q_{ur}^{K}),
\]

not a single presumed transition or personality operator. The population
opportunity object and observed phase object are:

\[
G_c^{set}=E_{u,r}[V_{ur}^{K}],
\qquad
C_c^{phase}
=
\operatorname{RepCov}_{u}(q_{u0}^{K},q_{u1}^{K}).
\]

The raw K statistic-level conditional order null found no sequence excess:
all D1/D2 HS/fidelity maxT \(p\) values were .762--.966. Orbit
standardization reached the same decision. D1 showed only uncorrected trends
(maxT \(p=.211/.249\)); D2 reversed relative to its null
(maxT \(p=.967/.955\)).

In contrast, \(G^{set}\) showed a robust PANDORA--Essays common orientation.
A block-Haar null preserved the four K feature blocks and their spectra while
rotating coordinates within each block. D1/D2 HS excess was .093/.159 and
fidelity excess was .116/.170; all four maximum-family \(p=.002\), with
positive author-bootstrap lower bounds.

That result was then subjected to an event-set composition knockout. Within
each corpus, split, context, and event-position slot, all event vectors were
reallocated without self-donors inside local text-length blocks. This
preserved every event marginal while destroying natural within-author
co-occurrence. The original EPSG scores were replayed to \(10^{-4}\), but
pseudo-set worlds reproduced them: D1 HS/fidelity maxT \(p=.635/.405\) and
D2 \(p=.750/.755\).

The operator hierarchy must therefore include an explicit marginal
background:

\[
G_c^{0}
=
E_{X_{1:4}\sim\nu_c^{context,slot}}
\operatorname{Cov}_{\pi\in S_4}[K(\pi X_{1:4})],
\]

\[
G_c^{real}
=
G_c^{0}+\Delta G_c^{composition}.
\]

The current cross-corpus overlap is licensed only for \(G_c^{0}\)-compatible
operator/marginal geometry. A shared \(\Delta G^{composition}\) and
\(C^{phase}\) were not detected.

The final corpus-local signed residual test also failed. Natural D0 sets
froze one rank-10 support per corpus; 99 D0 pseudo worlds estimated
context/replicate marginal baselines; D1/D2 used the Frobenius norm of signed
cross-replicate covariance without a positive-part projection. PANDORA maxT
\(p=.865/.800\), and Essays \(p=.995/.995\). Essays natural strengths were
below their pseudo means.

The final background object is therefore the conditional marginal
orbit-susceptibility operator:

\[
\mathcal T_{K,c}
=
E_Z E_{\prod_{j=1}^{4}P_c(x_j\mid Z,j)}
\left[
\operatorname{Cov}_{\pi\in S_4}
K(\pi\{x_1,\ldots,x_4\})
\right],
\]

where \(Z\) contains corpus, context, event slot, and the declared length
matching neighborhood. The resolved hierarchy is:

\[
(c,o)
\longrightarrow
P_c(x\mid Z)
\longrightarrow
\mathcal T_{K,c}
\longrightarrow
R_{u,r}^{composition}
\longrightarrow
q_{u,r}^{sequence}.
\]

Current evidence reaches \(\mathcal T_{K,c}\) only. Both author-composition
and observed-sequence increments are refused under the present design.

The strongest licensed real-text result is therefore:

> Under the frozen K map, PANDORA and Essays share a reproducible
> permutation-susceptibility background induced by the operator and
> context/slot-conditioned event marginals. Natural within-author composition
> does not add a detected common cross-corpus excess, and actual observed
> order does not add a detected common phase.

This is an operator calibration/background object, not an author condition,
response, dynamic personality axis, or psychological construct. It explains
why a highly significant cross-corpus orientation can coexist with failed
shuffle and composition tests: the stable object can be generated before
authors are grouped and before an observed order is selected. Since D1 and
D2 were already opened, this remains exploratory.

Primary records:

- `docs/V8_REALTEXT_RELATION_FIELD_PROTOCOL.md`
- `reports/V8_REALTEXT_RELATION_FIELD_REPORT.md`
- `docs/V8_SUPPORT_COVERAGE_PROTOCOL.md`
- `reports/V8_SUPPORT_CONTAINMENT_REPORT.md`
- `docs/V8_SCALE_FREE_RESOLUTION_PROTOCOL.md`
- `reports/V8_SUPPORT_RESOLUTION_SCALE_FREE.md`
- `docs/V8_SPECTRAL_ORDER_REPLAY_PROTOCOL.md`
- `reports/V8_SPECTRAL_ORDER_REPLAY.md`
- `docs/V8_ORDER_STATISTIC_NULL_PROTOCOL.md`
- `reports/V8_ORDER_STATISTIC_NULL.md`
- `docs/V8_PERMUTATION_ORBIT_PHASE_PROTOCOL.md`
- `reports/V8_PERMUTATION_ORBIT_PHASE.md`
- `docs/V8_EVENT_SET_COMPOSITION_KNOCKOUT_PROTOCOL.md`
- `reports/V8_EVENT_SET_COMPOSITION_KNOCKOUT.md`
- `docs/V8_CORPUS_LOCAL_COMPOSITION_RESIDUAL_PROTOCOL.md`
- `reports/V8_CORPUS_LOCAL_COMPOSITION_RESIDUAL.md`

### 4.8 Conditional background quotient and M concordance spectrum

The order-free M family requires a different terminal object from K. For
author event aggregates

\[
m(\mathcal X)
=
[\operatorname{mean},\operatorname{variance},
\operatorname{RFF},\operatorname{quantiles}],
\]

context/slot/local-length-matched pseudo authors define a corpus-local
conditional background \(\mathbb B_c\). Complete event vectors are preserved,
while natural grouping and cross-replicate author correspondence are broken.
The quotient coordinate is

\[
q_{cur}
=
\Omega_{\mathbb B_c}^{-1/2}
\{m(\mathcal X_{cur})-E_{\mathbb B_c}[m\mid Z]\}.
\]

The historical pseudo distribution used forced nonzero cyclic shifts. It was
a useful no-self knockout, but it excluded fixed points and the observed
identity assignment. A complete rerun with uniform within-block permutations
therefore supersedes its inferential interpretation. Under the corrected
exchangeable null, normalized signed trace identifies same-coordinate
concordance in all three corpora. PANDORA is weak but positive in both panels
(excess \(+.01650/+.01533\), lower bounds \(+.00180/+.00037\)).

The opened-panel CBQ-CS follow-up defines

\[
B_c
=
\frac{Q_{c0}^{\mathsf T}Q_{c1}
+Q_{c1}^{\mathsf T}Q_{c0}}{2n},
\qquad
W_c
=
\frac12(C_{c00}+C_{c11}),
\]

and solves

\[
B_cv_{cj}
=
\lambda_{cj}
\left(
W_c+\gamma\frac{\operatorname{tr}W_c}{d}I
\right)v_{cj}.
\]

Positive and negative spectra are retained separately. D0 pseudo worlds set
the extreme-eigenvalue and subspace-affinity thresholds; frozen directions
are then scored in D1/D2.

Before declared-opportunity filtration, the exchangeability-corrected
corpus-local ranks were:

- PANDORA: rank 0 at the current resolution;
- Essays: rank 0 because its two D0 candidates fail split-half affinity;
- X-market: three positive M axes plus one mean-free strict-shape axis,
  dominated by dispersion/variance;
- no corpus: a stable negative spectrum.

These were not shared factor counts. The registered opportunity sensitivity
analysis then materially changed the interpretation. D0-frozen opportunity,
template, and collection profiles leave signed concordance positive in all
three corpora, but a fresh spectrum audit resolves no stable global linear
axis in PANDORA, Essays, or X. The earlier X directions are therefore
opportunity-mediated historical axes rather than current author factors.

The axis-free follow-up compares U-centered author-by-author relation
matrices. Its corpus statistic is a context-stratified block-diagonal direct
sum, implicitly weighted by retained Frobenius pair mass; it does not contain
cross-context author relations. This aggregate finds short-scale RBF relation
correspondence in PANDORA and multi-scale correspondence in X, while Essays
remains scalar-only. Neither a linear KRC axis nor a paired RBF-minus-linear
nonlinear increment closes. Thus:

\[
\text{signed trace}
\not\Rightarrow
\text{stable factor chart}
\not\Rightarrow
\text{absence of reproducible relation geometry}.
\]

Opened-panel disaggregation resolves no complete relation geometry in any
individual context. It does not retest scalar concordance context by context.
The context-stratified aggregate is therefore not evidence of a
context-specific score or unified PANDORA geometry.

The integrated technical chain is:

\[
(c,o)
\longrightarrow
P_c(x\mid Z)
\longrightarrow
\begin{cases}
\mathcal T_{K,c}, & \text{transition background},\\
q_{cur}\longrightarrow(B_c,W_c)
\longrightarrow
\begin{cases}
\{\lambda_{cj},V_{cj}\}^{+/-}, & \text{global linear chart if resolved},\\
\widetilde K_{cr}, & \text{axis-free relation correspondence},
\end{cases}
& \text{order-free concordance}.
\end{cases}
\]

Neither branch currently licenses a named personality, emotion, cognition,
diagnosis, or cross-corpus common coordinate.

Primary records:

- `docs/V8_MARGINAL_BACKGROUND_QUOTIENT_PROTOCOL.md`
- `reports/V8_MARGINAL_BACKGROUND_QUOTIENT.md`
- `docs/V8_CONDITIONAL_CONCORDANCE_SPECTRUM_PROTOCOL.md`
- `docs/V8_CBQ_CONCORDANCE_SPECTRUM_THEORY.md`
- `reports/V8_CONDITIONAL_CONCORDANCE_SPECTRUM.md`
- `docs/V8_EXCHANGEABLE_BACKGROUND_AUDIT_PROTOCOL.md`
- `reports/V8_EXCHANGEABLE_BACKGROUND_AUDIT.md`
- `docs/V8_NUISANCE_FILTRATION_PROTOCOL.md`
- `reports/V8_NUISANCE_FILTRATION.md`
- `docs/V8_FILTERED_CONCORDANCE_SPECTRUM_PROTOCOL.md`
- `reports/V8_FILTERED_CONCORDANCE_SPECTRUM.md`
- `docs/V8_RESIDUAL_GEOMETRY_CORRESPONDENCE_PROTOCOL.md`
- `reports/V8_RESIDUAL_GEOMETRY_CORRESPONDENCE.md`
- `docs/V8_CONTEXT_GEOMETRY_TRANSPORT_PROTOCOL.md`
- `reports/V8_CONTEXT_GEOMETRY_TRANSPORT.md`
- `reports/V8_CONTEXT_TRANSPORT_RESOLUTION_FRONTIER.md`

## 5. Evidence lattice

Every claim must be placed on one row before interpretation:

| Level | Evidence type | Current use |
| --- | --- | --- |
| E0 | Code, schema, provenance, and refusal correctness | Necessary engineering gate; no scientific claim by itself. |
| E1 | Opened synthetic development or power study | Selects candidates and exposes boundaries; cannot confirm itself. |
| E2 | Prospectively sealed, independently randomized synthetic confirmation | Can establish existence and estimator recovery in the declared world only. |
| E3 | Source-disjoint or repeated real-text technical replication | Can establish technical author structure within its sampling design. |
| E4 | External psychological or behavioral construct validation | Required before naming an object personality, state, emotion, or behavior. |
| E5 | Independent population/domain replication and clinical validation | Required before diagnostic or applied clinical use. |

Passing E1 or E2 never implies E4. High author AUC can reflect stable topic,
platform, demographic, lexical, or group structure. Reliability does not
establish validity. Mathematical existence does not establish human
prevalence.

## 6. Current integrated ruling

The current V8 mathematical foundation supports the following:

1. a deterministic, auditable assessment architecture with explicit refusal;
2. separable choice, shared-condition response, state, reference, and
   uncertainty objects under declared designs;
3. identifiable synthetic geometry, topology, routing, reliability, and
   hierarchy mechanisms within their registered worlds;
4. a credible cross-family M3 development battery for detecting anonymous
   author structure missed by low-order summaries.
5. a synthetic typed-graph distinction between latent technical objects,
   measurement uncertainty/refusal, population relations, and external
   readouts;
6. a replicated-observation relation license, a separate unique-mode license,
   and an observable veto for materially condition-sensitive global relations.
7. a synthetic event-to-M3-to-context-relation seam with population coverage,
   statistic-specific local/between nulls, and explicit refusal under
   measurement-support non-invariance or underresolution.
8. real-text corpus-local scale-free resolution regions for anonymous
   marginal and transition density families in PANDORA and Essays.
9. an exploratory PANDORA--Essays common K permutation-susceptibility
   background attributable to the frozen operator plus event marginals.
10. corpus-local signed M concordance beyond the declared conditional marginal
    background in PANDORA, Essays, and X-market under a corrected
    exchangeability null.
11. opportunity-filtered, context-stratified axis-free author-relation
    correspondence at a short scale in PANDORA and multiple scales in
    X-market.
12. an explicit real-text information-budget refusal: context-stratified
    aggregate relation correspondence does not imply that one context, one
    author pair panel, or a cross-context transport is individually resolved.
13. a strictly nested information-budget audit showing that event count and
    event composition are distinct measurement facets. Cross-context
    twelve-minus-eight relation excess is positive under all 15 eight-event
    compositions in both held panels, but the registered primary contrast
    and every simultaneous band remain open.

It does not yet support:

1. a universal decomposition of all text variation;
2. a zero-residual theorem;
3. named latent personality dimensions;
4. stable same-person transport across real conditions and occasions;
5. clinical scoring, diagnosis, or treatment decisions;
6. a formal M3-V4 confirmation claim;
7. a transport map that makes scores comparable after support drift;
8. random-context superpopulation generalization beyond the four registered
   fixed contexts.
9. a cross-corpus spectral order, natural within-author composition excess,
   observed-order phase excess, or sequence-sensitive operator for the
   real-text density families.
10. a declared-opportunity-filtered low-dimensional global M axis in any
    current corpus.
11. a cross-corpus common M axis, a stable negative concordance spectrum, or a
    psychological interpretation of any CBQ-CS direction.
12. context-specific or same-author cross-context relation transport under
    the current PANDORA information budget.
13. a monotone event-count law, a universal minimum text budget, or
    composition-invariant parallel forms.

The correct project state is:

`V8_LOCAL_RESOLUTION_SUPPORTED__K_BRANCH_CLOSED_AT_CMOSO__M_SIGNED_CONCORDANCE_ALL_THREE__FILTERED_GLOBAL_AXES_NONE__PANDORA_SHORT_SCALE_AND_X_MULTI_SCALE_CONTEXT_STRATIFIED_RELATION_CORRESPONDENCE__CONTEXT_TRANSPORT_INFORMATION_BUDGET_UNDERRESOLVED__HUMAN_CONSTRUCT_VALIDITY_OPEN`

The independent M4 chart-applicability branch now adds one negative closure:
after scalar coverage failed as a utility gate, a fresh 432-cell
fixed-coverage experiment also found that the current 24-component
pre-response signature cannot identify `R/B0` relative utility under LOWO or
LORO. This does not change the V8 real-text results. It changes the conversion
rule: chart applicability is licensed prospectively at study-design level,
with dual-path sensitivity or refusal outside that class, rather than learned
post hoc for each cell.

## 7. Next formal evidence sequence

This section records outstanding confirmation and measurement work for
existing objects. It is not the overall mechanism-development roadmap. The
development mainline is the M4 tree in
`docs/SUICA_THEORY_ROUTE_INDEX.md`: mechanism composition, endogenous
opportunity, condition-manifold discovery, dynamic generators, multiscale
coarse-graining, residual mechanism discovery, and the
individual-to-relation bridge.

Four non-substitutable sequences remain:

1. For real-text M, preserve the relation estimand and obtain a fresh D3
   panel with enough same authors, contexts, and events to leave the current
   support frontier. Register both event count and event-composition sampling
   before opening it. The current RGC, transport, and nested-budget results
   reuse opened D1/D2 panels and form one adaptive exploratory chain.
2. Instantiate the generic foundation interfaces specified in
   `docs/SUICA_FOUNDATIONAL_CLOSURE_STANDARD.md`: the declared
   generalization universe, relation-valued error study, frozen
   external-reference operator, and conditional uncertainty. These are
   prerequisites only when a study crosses from L4 technical structure to L5
   measurement; they are not a command to build a scale. The opened synthetic
   implementation is now documented in
   `docs/SUICA_L45_REFERENCE_FRAME_PROTOCOL.md`. Its V1 failure localized
   omitted occasion uncertainty; the repaired V3 passed all discovery gates
   but remains an L4-licensed candidate pending sealed confirmation and a
   real study-specific G/D design.
3. For M3-V4, commit and verify the complete implementation, create the clean
   pre-randomness seal, attach external randomness, generate encrypted truth
   and public observations, fit without truth access, and open once without
   retuning.
4. Only after technical confirmation may a separately registered external
   psychological or behavioral linkage study name any direction. A future
   applied instrument must additionally declare its construct, target
   population, behavioral domain, intended use, response process, invariance,
   fairness, and consequences.

Code commits make the route reproducible. They do not themselves constitute a
scientific seal or open the remaining lockbox. The next unopened seal remains
M3-V4; the SEAM-1/1A partials and SEAM-1B pass are opened synthetic
experiments and must not be relabelled as that confirmation. V8 as a whole
remains an active development framework.

The live list of constitutional and empirical openings is
`docs/SUICA_FOUNDATION_GAP_LEDGER.md`. A new experiment must localize one
opening in a validated foundation contract; an untyped opening is a refusal,
not a reason to spend data or compute.
