# SUICA V8 Mathematical Research Route and Evidence Archive

Status:
`INTEGRATED_DEVELOPMENT_ARCHIVE__M3_V4_READY_FOR_CLEAN_SEAL__PSYCHOLOGICAL_MAPPING_CLOSED`

Date: 2026-07-28

This document is the authoritative route map for the mathematical research
developed after the sealed `v0.2.1` release. It integrates the V8 assessment,
conditional-response, geometry, reliability, limit, residual-completion, and
M3 micro-to-meso lines into one framework.

It is not a new release seal. It does not reopen the `v0.2.1` lockbox, promote
development results to confirmation, or claim that an anonymous mathematical
object is personality.

## 1. Scientific target

SUICA is intended to become a general foundation for constructing
natural-language psychological measurements. It is not itself a completed
personality scale, symptom scale, diagnostic system, or clinical instrument.

The V8 target is to identify and estimate reproducible author-relative
structure in text while preserving four separations:

1. observed text events versus latent author process;
2. author process versus measurement error;
3. technical author structure versus psychological interpretation;
4. deterministic measurement versus LLM explanation.

The numerical channel remains deterministic. A frozen LLM may convert
registered evidence into constrained human-readable explanations, but it may
not invent scores, constructs, diagnoses, validity, or certainty.

## 2. Unified mathematical object

For author \(u\), opportunity \(o\), condition \(c\), occasion \(t\), and
observer \(r\), the V8 mesoscopic observation model is

\[
c_{ui}\sim\pi_u(c\mid o_{ui}),
\]

\[
y_{uictr}
=
\mu_{c,o}+a_u+
\left[B_0+F(\eta_u)+\varepsilon D_u\right]z_c
+Hs_{ut}+\delta_r+e_{uictr}.
\]

Its technical measurement object is

\[
\mathfrak M_u
=
\left(
a_u,\pi_u,[B_u],R_u^{\mathrm{stable}},
U_u,E_u;\mathcal P_{\mathrm{ref}}
\right).
\]

The M3 line supplies the microscopic process beneath that object:

\[
\mathcal N_u
=
\sum_{o,t}
\delta_{(o,t,O_{uot},C_{uot},S_{uo},\Xi_{uot})}.
\]

The integrated route is therefore

\[
\mathcal N_u
\xrightarrow{\mathcal C}
\mathfrak A_u
\xrightarrow{\mathcal E_m}
\widehat{\mathfrak M}_u^{(m)}
\xrightarrow{\Psi}
\mathcal Y_u.
\]

The anonymous mesoscopic author object is

\[
\mathfrak A_u
=
\left(
\pi_u,G_u,a_u,[B_u],h_u,\mathcal K_u,\mathcal C_u,\mathcal P_u
\right).
\]

Here:

- \(\mathcal C\) is a family of coarse-graining maps from event paths to
  mesoscopic author objects;
- \(G_u\) is the condition-indexed author-relative response field;
- \(a_u\) and \([B_u]\) are its stable position and response-operator
  equivalence class;
- \(h_u\) is unresolved nonlinear structure outside the registered response
  family, not automatically noise;
- \(\mathcal K_u\) is a temporal or transition operator;
- \(\mathcal C_u\) is an interactional coupling object;
- \(\mathcal P_u\) is higher-order path structure;
- \(\mathcal E_m\) is a finite-resolution estimator with uncertainty and
  refusal;
- \(\Psi\) is the external construct mapping.

The symbol \(\Psi\) remains closed. V8 currently studies the existence,
identification, estimation, geometry, and reliability of anonymous technical
objects. Psychological naming requires external evidence designed for that
construct.

## 3. Why M3 belongs inside V8

M3 is not a separate product line. It is V8's micro-to-meso existence and
identification layer.

Earlier V8 work asked whether author-relative positions, response operators,
incidence structures, routing kernels, and residual channels can be measured
under declared designs. M3 asks the complementary question:

> Can different microscopic text-path laws generate stable author structure
> that ordinary means, low-order moments, or short-lag covariance cannot
> identify, while a fixed generator-blind measurement battery recovers it and
> refuses exact aliases?

M3-V4 operationalizes three anonymous object families:

- distribution geometry beyond degree-four moments;
- nonlinear response operators beyond a joint polynomial nuisance space;
- path dynamics beyond low-order covariance, including dwell hazard,
  probability current, and nonlinear variance dynamics.

These are candidate mechanisms for the middle of the V8 map. They are not
named factors and are not evidence that any recovered coordinate is a
personality trait.

## 4. Research route

### Route A: evidence-carrying assessment

V8.0-V8.4 established the executable contract, provenance chain, explicit
refusal rules, explanation certificates, and the role boundary between
deterministic measurement and LLM rendering.

The LLM semantic channel recovered planted events accurately but failed the
registered stability/increment gates on real text. The accepted architecture
therefore keeps the LLM as an evidence renderer rather than a numeric scorer.

Primary records:

- `docs/V8_LLM_NATIVE_ASSESSMENT_THEORY.md`
- `docs/V8_EXECUTION_PLAN.md`
- `reports/V8_FULL_TECHNICAL_EXPERIMENT_REPORT.md`
- `reports/V8_FULL_PROVENANCE_AUDIT.md`

### Route B: conditional response and author geometry

The canonical response geometry separates:

- C1 selected exposure, including entered, avoided, revisited, or created
  opportunities;
- C2 response under shared conditions;
- stable author position;
- occasion state, observer variation, and finite-text error;
- population, group, and within-group individual structure.

Canonical geometry improved source-disjoint author discrimination within
PANDORA, but the first explicit behavior bridge did not support a stable
cross-modal link. The geometry and behavior channels must therefore remain
separate until a registered bridge succeeds.

Primary records:

- `docs/V8_CONDITIONAL_RESPONSE_GEOMETRY_THEORY.md`
- `reports/V8_CANONICAL_ORBIT_GEOMETRY_REPORT.md`
- `reports/V8_CANONICAL_BEHAVIOR_BRIDGE_REPORT.md`
- `reports/V8_BEHAVIOR_C2_PLANTED_WORLD_REPORT.md`

### Route C: sets, topology, and path dynamics

The V3.0-V3.7 sequence progressively replaced point-only descriptions with
condition-indexed response sets and path operators:

1. response-set incidence distinguished same-condition from free-image
   proximity;
2. persistent hyperedges represented simultaneous multi-author feasibility;
3. condition indices were shown to carry information lost by aggregate pair
   matrices;
4. common-ball birth scales distinguished pairwise adjacency from joint
   feasibility;
5. local transversality separated registered contact types;
6. spacetime routing separated junction geometry from transition choice;
7. author-specific routing operators introduced group-conditioned scoring,
   blind localization, transport, adaptive rank, and fixed reference panels.

These results are synthetic technical identification results. Intersections,
routes, clusters, rings, residuals, and dynamic axes are not psychological
constructs without external validation.

### Route D: reliability, resolution, and zero limits

V3.7G-V3.7H.4 asked which apparent residuals are finite-resolution
measurement effects and which survive declared limits.

The core progression is:

\[
\mathcal F_{m_1}\subset\mathcal F_{m_2}\subset\cdots,\qquad
\widehat{\mathfrak M}_u^{(m)}
=
\mathbb E[\mathfrak M_u\mid\mathcal F_m]
\quad\text{only under a justified estimator model}.
\]

The experiments established narrower results:

- V3.7G: a core observable reliability spectrum passed in its registered
  synthetic setting, with an NMSE/NRMSE terminology erratum;
- V3.7H: resolution-filtration discovery completed but was not confirmed;
- V3.7H.1: risk contracted, but the process was not licensed as a posterior
  martingale;
- V3.7H.2: repeated opportunities separated common shock from technical
  replication, subject to a replication boundary;
- V3.7H.3: multiscale zero identification partially passed in an additive
  balanced world;
- V3.7H.4: misspecification tests refuted an overconfident additive
  interpretation.

The accepted lesson is that "more text" and "more authors" can reduce
specific error components, but they do not force every unresolved component
to zero.

### Route E: reference measure and residual completion

V3.7H.4D and R1-R2G.2 tested whether residual structure can be localized,
predicted, or completed under different reference and hierarchy limits.

The sequence is intentionally preserved with its failures:

| Stage | Current ruling | What it changed |
| --- | --- | --- |
| H.4D | `SMOKE_REFUTED_IDENTIFIABILITY` | An absolute post-centering interaction target was algebraically invalid. |
| R1 | `PARTIAL_REFERENCE_CONTRAST` | Reference contrasts were recoverable, minority sensitivity remained unresolved. |
| R2B | `PARTIAL_OPERATOR_PREDICTIVE_NOT_SUFFICIENT` | Geometry predicted detector power but did not fully explain it. |
| R2C | `REFUTED_PERMUTATION_ORBIT_EXPLANATION` | Cell calibration worked; individual conditional prediction did not. |
| R2D | `STOP_NO_EXPLOITABLE_CONDITIONAL_TARGET` | The registered process was primarily cell-level. |
| R2E.2 | `CONFIRMED_NORMAL_ONLY` | One normal direction was active; the registered tangent family was practically null. |
| R2F.1 | `STOP_NO_LOCAL_TANGENT_CANDIDATE` | A valid search found no material stable local candidate. |
| R2G | `INCONCLUSIVE_BOUNDED_COMPLETION` | Four score opportunities were insufficient for practical zero. |
| R2G.1 | `PARTIAL_CONSTRUCTIVE_EXISTENCE` | Known completable structure approached the factor-completeness limit as score resolution increased. |
| R2G.2/A | `SUPPORTED_SYNTHETIC_HIERARCHICAL_FORMULA_WITH_IDENTIFIABILITY_BOUNDARY` | Society, group, and author precision terms were separated under the registered hierarchy. |

This route supports a conditional statement: some residual floors are
finite-information effects and some are structural aliases or omitted
mechanisms. It does not support the universal claim that the true residual is
zero.

### Route F: M3 cross-family confirmation

M3-V1 is retained as a matched-family engineering baseline, not confirmation.
M3-V2 is a basis-blinded near-family discovery with partial nonlinear
generalization. M3-V4 replaces that weak bridge with cross-family planted
objects and one generator-blind estimator battery.

The 12-seed V4 preflight passed all 17 registered development gates:

- CF-D expected AUC 0.636-0.680 and geometry 0.331-0.468;
- CF-O expected AUC 0.99991-0.99997 and geometry 0.977-0.988;
- CF-KP expected AUC 0.758-0.809 and geometry 0.829-0.876;
- aliases remained at AUC 0.4984-0.5008;
- six null families had maximum mean AUC deviation 0.0119;
- target-knockout geometry magnitude was at most 0.0419.

This result licenses a clean pre-randomness seal. It is not formal
confirmation. In particular, the ARCH proper held-out gain is approximately
\(2.27\times10^{-6}\), so the unopened confirmation may legitimately fail.

Primary records:

- `docs/SUICA_M3_MICRO_MESO_MACRO_FOUNDATION.md`
- `docs/SUICA_M3_MECHANISM_ATLAS_THEORY.md`
- `docs/SUICA_M3_CROSS_FAMILY_CONFIRMATION_PROTOCOL_V4.md`
- `reports/SUICA_M3_CROSS_FAMILY_PREFLIGHT_V4.md`

## 5. Evidence lattice

Every claim must be placed on one row before interpretation:

| Level | Evidence type | Current use |
| --- | --- | --- |
| E0 | Code, schema, provenance, and refusal correctness | Necessary engineering gate; no scientific claim by itself. |
| E1 | Opened synthetic development or power study | Selects candidates and exposes boundaries; cannot confirm itself. |
| E2 | Prospectively sealed, independently randomized synthetic confirmation | Can establish existence and estimator recovery in the declared world only. |
| E3 | Source-disjoint or repeated real-text technical replication | Can establish technical author structure within its sampling design. |
| E4 | External psychological or behavioral construct validation | Required before naming an object personality, state, emotion, or behavior. |
| E5 | Independent population/domain replication and clinical validation | Required before diagnostic or applied clinical use. |

Passing E1 or E2 never implies E4. High author AUC can reflect stable topic,
platform, demographic, lexical, or group structure. Reliability does not
establish validity. Mathematical existence does not establish human
prevalence.

## 6. Current integrated ruling

The current V8 mathematical foundation supports the following:

1. a deterministic, auditable assessment architecture with explicit refusal;
2. separable choice, shared-condition response, state, reference, and
   uncertainty objects under declared designs;
3. identifiable synthetic geometry, topology, routing, reliability, and
   hierarchy mechanisms within their registered worlds;
4. a credible cross-family M3 development battery for detecting anonymous
   author structure missed by low-order summaries.

It does not yet support:

1. a universal decomposition of all text variation;
2. a zero-residual theorem;
3. named latent personality dimensions;
4. stable same-person transport across real conditions and occasions;
5. clinical scoring, diagnosis, or treatment decisions;
6. a formal M3-V4 confirmation claim.

The correct project state is:

`V8_TECHNICAL_MATHEMATICAL_FRAMEWORK_INTEGRATED__M3_V4_AWAITING_CLEAN_SEAL__HUMAN_CONSTRUCT_VALIDITY_OPEN`

## 7. Next formal sequence

The next sequence is fixed:

1. commit the complete V8/M3 implementation, protocols, audits, and this
   archive in reviewable batches;
2. verify a clean worktree, source inventory, JSON/config parsing, and the
   complete test suite;
3. create the M3-V4 pre-randomness seal from the exact formal config;
4. attach external randomness only after the seal exists;
5. generate encrypted truth and public observations;
6. fit without generator or truth access;
7. open once, verify the hash chain, run independent mathematical validity,
   and publish the formal decision without retuning;
8. only after a synthetic pass, design real repeated/fixed-condition tests
   that can distinguish technical author structure from psychological
   constructs.

Code commits make the route reproducible. They do not themselves constitute a
scientific seal or open the remaining lockbox. The next seal is M3-V4 only;
V8 as a whole remains an active development framework.
