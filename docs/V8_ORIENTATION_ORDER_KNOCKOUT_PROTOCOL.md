# V8 K-Family Orientation Order-Knockout Protocol

Status: `EXPLORATORY_FALSIFICATION_PROTOCOL`

## Question

The K-family orientation candidate was built from ordered transitions, but
that does not prove that its cross-corpus overlap depends on order. This
control holds fixed each author, event multiset, even/odd technical replicate,
event count, corpus, and context while permuting event order inside each
replicate.

The primary paired contrast is

\[
\Delta_{\mathrm{order}}
=
S(\Sigma_{K,P},\Sigma_{K,E})
-
S(\Sigma_{K^\pi,P},\Sigma_{K^\pi,E}).
\]

Matched-spectrum HS and root fidelity must both have positive paired
bootstrap lower bounds in D1 and D2. The shuffled arm is also compared with a
spectrum-preserving Haar null.

## Boundary

A pass permits `ORDER_SENSITIVE` for this anonymous K-family orientation
candidate under the current opened operator and authors. It does not identify
a psychological dynamic, thought process, personality factor, universal
language mechanism, diagnosis, or clinical effect. Fresh D3 or new data are
required for independent confirmation.
