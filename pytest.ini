[pytest]
; The canonical suite lives in tests/. Never collect from results/ —
; executor scan machinery (e.g. #83 HEAD-baseline copies) may place
; stale test-file copies there that cannot resolve their own paths.
testpaths = tests
norecursedirs = results artifacts logs .git
