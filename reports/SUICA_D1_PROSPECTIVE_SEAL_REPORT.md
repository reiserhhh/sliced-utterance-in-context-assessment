# SUICA D1 — Prospective seal #2 (report)

Banner: **prediction arithmetic on published constants; NO world of any kind is generated by this leg**

Registration: `docs/SUICA_DEFENSE_PHASE_PLAN.md` section "D1 — Prospective seal #2" (REGISTERED 2026-08-10, BEFORE RUN, commit `4dad82e`). Charter: `docs/SUICA_DEFENSE_PHASE_CHARTER.md`. Executor standing: implementation and execution only.

**This report never carries a sealed value.** It carries the protocol, the provenance, the purity attestation, the entry STRUCTURE (which entries, which band rules), the HASH, and the opening protocol. The values live only in the gitignored bundle.

Part 0 written at `2026-08-09T17:41:45.069315+00:00` — BEFORE the seal was produced.

---

## Part 0.1 — Protocol authority and the disclosed deviation

OP-31 ("prospective seal #1, the time lockbox") was LOCATED:

- human copy of record: `docs/OP31_PROSPECTIVE_SEAL.md` (tracked; carries `self_sha256` and the plaintext predictions)
- machine copy / harness: `scripts/run_suica_op31_prospective_seal_v1.py` (the `PREDICTIONS` dict at op31:49-61, the sealing mechanic at op31:64-65, the tamper check at op31:84-85)
- OP-31's own artifact directory `results/suica_op31_prospective_seal_v1/PROSPECTIVE_SEAL.json` does NOT exist on this machine, and `results/` is gitignored in full (`.gitignore:2`), so OP-31's seal of record is in practice the tracked DOC plus the tracked script constant.

**Mechanic ADOPTED from OP-31 (op31:64-65):** the content hash is `sha256(json.dumps(obj, sort_keys=True).encode()).hexdigest()` over the canonical, key-sorted JSON of the prediction object. D1 uses exactly this, with the salt prefixed to the canonical JSON.

**Concealment model INCOMPATIBLE — deviation disclosed.** OP-31 could commit its predictions IN PLAINTEXT because they concerned text not yet written: no one could peek, so no one could back-fit. D1's five configurations are runnable TODAY. Publishing them in plaintext would destroy the seal's whole value. D1 therefore takes the registration's PRE-DECLARED FALLBACK (`docs/SUICA_DEFENSE_PHASE_PLAN.md:24-28`): salted SHA-256; the sealed plaintext bundle under `results/d1_sealed/` (gitignored); ONLY the hash, the salt-handling statement, the provenance table and the opening protocol are committed. The hash MECHANIC is OP-31's; the STORAGE model is the fallback's. That split is the deviation.

**Salt handling.** The salt is 32 cryptographically random bytes (`secrets.token_hex(32)`), generated once at seal time. It is stored ONLY inside the gitignored bundle, next to the plaintext it salts. It is never committed, never printed to the terminal, and never quoted in this report. Its only purpose is to make the committed hash non-invertible by dictionary search over plausible prediction tables — without it, an adversary who guessed the schema could brute-force five numbers. Losing the salt voids the seal exactly as losing the bundle does.

**COPY INSTRUCTION FOR THE PROGRAM OWNER (required — the bundle is gitignored and therefore unbacked):**

```
cp -p 'results/d1_sealed/D1_SEALED_BUNDLE.json' <somewhere outside this repo, e.g. an encrypted volume or a password manager note>
```

Keep at least one copy off this machine. The D-open leg needs the bundle AND its salt; this report's hash is only the tamper check.

## Part 0.2 — G0D provenance table (every constant round-trip parsed)

| constant | file | field | round-trip value | registration literal | residual | bit-exact |
|---|---|---|---|---|---|---|
| `lambda` | `results/m4_k2d_frontier_carrier/decision.json` | `leans/q_update/lambda` | `0.17417497661611914` | `0.17417497661611914` | `0.0` | **YES** |
| `q` | `results/m4_k2d_frontier_carrier/decision.json` | `leans/q_update/q` | `1.8528700746510731` | `1.8528700746510731` | `0.0` | **YES** |
| `kappa_hat` | `results/m4_k2d_frontier_carrier/post_hoc_descriptive.json` | `kappa_ols_through_origin` | `-0.7220359963712748` | `-0.7220359963712748` | `0.0` | **YES** |
| `kappa_hat_k2e_reverify` | `results/m4_k2e_double_matching/decision.json` | `G0e_anchors/kappa_companion/kappa_rederived` | `-0.7220359963712748` | `-0.7220359963712748` | `0.0` | **YES** |
| `kappa_resid_max_9pair` | `results/m4_k2e_double_matching/decision.json` | `kappa_refit_9pairs/max_abs_residual` | `0.008917886817207595` | `0.008917886817207595` | `0.0` | **YES** |
| `delta_L1` | `results/m4_l1_typed_world/decision.json` | `delta` | `5.928950380606693` | `5.928950380606693` | `0.0` | **YES** |
| `sigma_b2_rho55eq` | `results/m4_l3_taxometer_meter/decision.json` | `sigma_b2/rho55eq` | `16.111540782194115` | `16.111540782194115` | `0.0` | **YES** |
| `sigma_b2_rho35eq` | `results/m4_l3_taxometer_meter/decision.json` | `sigma_b2/rho35eq` | `7.098091393554051` | `7.098091393554051` | `0.0` | **YES** |
| `delta_amb03_L2` | `results/m4_l2_threshold_continuum/gates.json` | `G2M/rule18_joint/clause_delta_intervals/W-1a (ambient < 0.30)/1` | `2.6977587972014083` | `2.6977587972014083` | `0.0` | **YES** |
| `delta_orc80_L2` | `results/m4_l2_threshold_continuum/gates.json` | `G2M/rule18_joint/clause_delta_intervals/W-1a (oracle-S > 0.80)/0` | `4.238555648405859` | `4.238555648405859` | `0.0` | **YES** |
| `k_latent_L2` | `results/m4_l2_threshold_continuum/gates.json` | `k_latent` | `48` | `48` | `0.0` | **YES** |
| `n_authors_L2` | `results/m4_l2_threshold_continuum/gates.json` | `n_authors` | `512` | `512` | `0.0` | **YES** |
| `k_tau_L2` | `results/m4_l2_threshold_continuum/gates.json` | `k_tau` | `3` | `3` | `0.0` | **YES** |
| `taxometer_tolerance` | `results/m4_l3_taxometer_meter/decision.json` | `leans/X-2/tolerance` | `0.125` | `0.125` | `0.0` | **YES** |

Two provenance corrections to the registration text, disclosed rather than silently followed:

1. **κ's persisted home.** The registration cites `results/m4_k2d_frontier_carrier/decision.json (kappa fit)`. That file contains NO κ field. The fit lives in the sibling `results/m4_k2d_frontier_carrier/post_hoc_descriptive.json` under `kappa_ols_through_origin`, and K2e re-derived it bit-exactly (`G0e_anchors/kappa_companion/kappa_rederived`, residual `0.0`). Same leg, same number, different file — the table above cites the real field and carries the K2e re-verification as its own row.
2. **κ's SIGN.** Every persisted field stores `-0.7220359963712748`; the registration writes `κ = 0.7220359963712748` in the law `field ≈ λ·r^q − κ·V_person`, i.e. κ is the positive magnitude of a SUBTRACTED tax. This leg uses the persisted signed value with a `+`, which is the same arithmetic; the convention is pinned as RN-D1-9.

**Unavoidable overlap between the provenance table and one sealed quantity, disclosed.** G0D requires the constants above to be committed. Two of them — `delta_orc80_L2` and `delta_amb03_L2` — are ALSO, by the S-3 formula and with no arithmetic in between, S-3's predicted lower edge (which is `(d, n)`-invariant by appendix Q.1) and S-3's predicted upper edge AT THE BASELINE SETTING `(48, 512)`. The registration itself already labels that setting "known empty", so nothing secret is disclosed by the coincidence. S-3's genuinely sealed content is the `(192, 256)` setting: its scale factor, its upper edge, its width and its OPEN/EMPTY call appear nowhere in this report. A mechanical leak check enforces this and is reported in Part 1.

### Source-object constants (drift check against the persisted files)

| source object | file:line | value | matches persisted |
|---|---|---|---|
| `l2.L1_DELTA` | `scripts/run_suica_m4_l2_threshold_continuum.py:111` | `5.928950380606693` | **YES** |
| `l2.SB2_RHO55` | `scripts/run_suica_m4_l2_threshold_continuum.py:128` | `16.111540782194115` | **YES** |
| `l2.SB2_RHO35` | `scripts/run_suica_m4_l2_threshold_continuum.py:129` | `7.098091393554051` | **YES** |
| `l2.K_TAU` | `scripts/run_suica_m4_l2_threshold_continuum.py:102` | `3` | **YES** |
| `k2a.K_LATENT` | `scripts/run_suica_m4_k2a_expressive_world.py:89` | `48` | **YES** |
| `l1.SIGMA_TAU2_PER_DELTA2` | `scripts/run_suica_m4_l1_typed_world.py:194` | `0.375` | **YES** |

### G0D, second half — the FORMULAS round-tripped, not just the numbers

S-4 is the only entry whose value comes out of executable published machinery rather than closed-form arithmetic, so the machinery itself is round-tripped: the K2c pair arms are re-derived through the very functions S-4 calls and compared bit-exactly with what K2c/K2d wrote to disk.

| arm | share | φ | r persisted | r re-derived | bit-exact | V_person persisted | V_person re-derived | bit-exact |
|---|---|---|---|---|---|---|---|---|
| `P1a` | `0.10921276830855525` | `0.9` | `0.78` | `0.78` | **YES** | `0.03276383049256658` | `0.03276383049256658` | **YES** |
| `P2a` | `0.29267462506992153` | `0.9` | `0.68` | `0.68` | **YES** | `0.0878023875209765` | `0.0878023875209765` | **YES** |
| `P3a` | `0.4973617623232523` | `0.9` | `0.5600000000000002` | `0.5600000000000002` | **YES** | `0.1492085286969757` | `0.1492085286969757` | **YES** |

## Part 0.3 — G1D purity statement (enforced in code)

The registration's G1D: *THIS LEG GENERATES NO WORLD of any kind. Prediction arithmetic only. Any world generation is a defect → STOP.*

Enforcement, not assertion:

1. **Raising stubs.** Before any entry is computed, every world / panel / card generator in the reuse graph is REPLACED by a stub that raises `SystemExit`. A call would be a crash, not a silent pass. Guards installed this run: `k2a.build_world`, `k2a.response_panel`, `k2a.card`, `k2a.centered_channels`, `l1.build_typed_world`, `l1.type_geometry`, `l1.latent_identity`, `l1.cards_for_variant`, `l1.run_world_cells`, `l2.type_geometry_l2`, `l2.latent_identity_l2`, `l2.typed_trait_l2`, `l2.cards_for_cell`, `l2.build_typed_world_l2`, `l2.measure_cell_world`.
2. **RNG attribution.** `np.random.default_rng` and `np.random.seed` are wrapped by a logger that reads the IMMEDIATE calling frame and records it only if that frame is a SUICA file. Immediate-frame attribution is deliberate: `scipy.stats` calls `default_rng(615681484984984)` dozens of times at import while building distribution docstrings, and a first-non-library stack walk would blame whichever SUICA line happened to `import scipy.stats`. Foreign calls are counted, not hidden. The full SUICA log and the foreign count are reproduced in Part 1 and stored in the bundle.

**RN-D1-3 (rule 9) — why the LATENT floor and not `l2.predicted_boundary_error_l2`.** L2's floor function is the CARD-SPACE realization: it needs `world['loadings']` and a realized type geometry, i.e. a generated world. Calling it would violate G1D. Its own docstring (l2:391-393) states that the card form *"reduces to derivation 2's `sigma_u^2(eta) = eta sigma_b^2/k_tau + (1-eta) sigma_b^2/m` exactly when M is an isometry"*. S-1 therefore seals the LATENT law — the theorem's own object (appendix R.1 / Q.1) — and the D-open leg must adjudicate against the latent form on a realized world, which is precisely the containment question L2's W-3 already answers 7/10 at its own config.

**RN-D1-4 (rule 9) — what G=5 and n_occ=8 do and do not change.** For a regular simplex every pairwise boundary sits at `Delta/2` regardless of the number of vertices, so `G` changes the COUNT of boundaries (`C(5,2)=10`) and not the PER-BOUNDARY rate S-1 predicts. `n_occ` enters only the FULL-Bayes rate's slow/observation terms (l2:411-414); the identity-only FLOOR — the registered object — does not see it. Note also that the registered config is internally consistent: `k_tau = G - 1 = 4` is exactly the dimension a 5-vertex regular simplex spans, as `k_tau = 3` was for L1/L2's `G = 4`.

**RN-D1-1/2 (rule 9) — what "identity energy at the ρ.55-equivalent" pins.** L2 introduced "ρ.55-equivalent" as a TERM OF ART for a fixed NUMBER, decoupled from Δ (l2:124-129: *"L2 keeps Delta free and pins sigma_b^2 at the two ENERGIES L1 realized at its own Delta"*). S-1's PRIMARY reading therefore transports `sigma_b^2 = SB2_RHO55` and `Delta = L1_DELTA` UNCHANGED to the new `(m, k_tau)`, exactly as L2's C-arm cells use them. Because the two readings of the phrase are both defensible, the second — the Δ-free `rho_id = 0.55` form made internally consistent with G=5's own simplex constant `c_5 = (G-1)/(2G) = 0.4` — is computed and sealed as a NAMED COMPANION inside S-1. The PRIMARY is named in the bundle; the companion cannot be promoted at opening time.

**RN-D1-5 (rule 9) — S-3's `d`.** The registration's baseline setting `(48, 512)` is labelled "known empty", and L2's own measured window IS empty (`W1_window.satisfiable_anywhere_in_delta = false`) at `k_latent = 48`, `n_authors = 512`. `d` is therefore pinned as the LATENT working-space dimension `m` (k2a:89), not the card dimension `DIM = 64` (k2a:90) that L2's W-2 row used for its own `gamma`. The pin makes the registration's "known empty" label true by measurement rather than by extrapolation.

## Part 0.4 — the band rules (rule 19: each band on the law's OWN quantity), pinned before any value

| entry | law's own quantity | band rule | rule-9 note |
|---|---|---|---|
| S-1 | a per-boundary ERROR RATE | RELATIVE band = `2.0` × (max relative deviation of measured-from-predicted over L2's ten-cell W-3 containment record, restricted to cells with predicted rate ≥ `0.001`). Cells whose predicted rate is ≤ `1e-06` are numerically degenerate and get an ABSOLUTE band `0.001` instead. | RN-D1-6. The restriction is stated, not fitted: the sub-1e-6 cells have no meaningful relative deviation (L2 measured exactly `0.0` at both η=0 cells), and including them would make the band infinite. |
| S-2 | a RATIO and an ORDER | no numeric tolerance — the band is the ARI-drop DIRECTION plus a strict ordering claim in η, with a `1e-12` algebraic tolerance on the ratio itself. | RN-D1-9. S-2 is exact algebra; a numeric band would be a category error. |
| S-3 | a Δ-INTERVAL (window edges and width) | interval predictions with a relative band `2.0` × S-1's on EACH edge, plus a BINARY open/empty call as the primary falsifier. | RN-D1-7. The multiplier is derived, not chosen: the `(d/n)^(1/4)` law has never been tested ACROSS `(d, n)` in this program, only within one setting, so its band is twice the measured floor law's. |
| S-4 | a FIELD value | ABSOLUTE band = `2.0` × the max ABSOLUTE RESIDUAL of the 9-pair K2c+K2d+K2e κ refit. | RN-D1-8. |
| S-5 | an η̂ READING | ABSOLUTE band = the CERTIFIED `±0.125` (L3 X-2, 10/10 within), with the known `+O(0.05)` pole-bias CARRIED as a note: η=0.6 is interior, so the bias is not applied to the point prediction. | RN-D1-10. |

The band constants are all DERIVED from persisted records at run time. The only free choices are the two safety factors and the S-3 multiplier, all fixed above before any value existed.

---

## Part 1 — the seal

### The entry structure (NO values — the values are sealed)

| entry | grade | configuration (from the registration) | what is sealed | band kind |
|---|---|---|---|---|
| **S-1** | MEASURED-LAW | `{"G": 5, "energy": "rho.55-equivalent", "eta_levels": [0.0, 0.5, 1.0], "k_tau": 4, "m": 96, "n_occ": 8}` | 3 per-boundary error rates (η ∈ {0, 0.5, 1}), each with σ_u²(η) and the boundary z, plus a named Δ-free companion | relative (derived from L2's W-3 record) / absolute for the degenerate cell |
| **S-2** | EXACT-ALGEBRA | `{"k_tau": 4, "m": 96}` | the z-ratio and z²-ratio, plus the ARI-drop direction claim | sign + strict ordering (no numeric tolerance) |
| **S-3** | CONJECTURE-GRADE | `{"baseline_d_n": [48, 512], "labels": ["known empty", "conjectured open"], "settings": [[48, 512], [192, 256]]}` | 2 settings × (lower edge, upper edge, width, OPEN/EMPTY call) | interval, conjecture-grade + binary open/empty |
| **S-4** | MEASURED-LAW | `{"instrument": "K2b (985-author K1-pinned panel, F2 m-multiset, 4 contexts)", "phi": 0.9, "share": 0.4, "w_int_arm": "zero"}` | the predicted b-only field recovery, its level term, and the (r, V_person) inputs | absolute (derived from the 9-pair κ residual record) |
| **S-5** | MEASURED-LAW (certified instrument) | `{"energy": "rho.45-equivalent", "eta": 0.6}` | the predicted η̂ reading and the cell's σ_b² | absolute (the certified ±0.125) + carried pole-bias note |

### Formulas, by source object (rule 12)

- **S-1** — per-boundary rate = Phi(-Delta/(2 sigma_u(eta))), sigma_u^2(eta) = eta sigma_b^2/k_tau + (1-eta) sigma_b^2/m
  - source: scripts/run_suica_m4_l2_threshold_continuum.py:384-416 (predicted_boundary_error_l2 docstring's LATENT reduction, l2:391-393) and l2:934-936; IDT appendix R.1 / Q.1
- **S-2** — z(eta=0)/z(eta=1) = sigma_u(1)/sigma_u(0) = sqrt(m/k_tau) -- Delta-free, rho-free, G-free
  - source: scripts/run_suica_m4_l1_typed_world.py:197-206 (identity_only_z: 'the SNR factor m/k_tau of R2 is the ratio of the two z^2')
- **S-3** — BBP energy criterion: a between-type spike lambda = Delta_card^2/8 is found against bulk sigma^2 iff lambda > sigma^2 sqrt(d/n), so the AMBIENT break scales as Delta ~ sigma (d/n)^(1/4); the ORACLE-S / Bayes-ceiling edge is projection-INVARIANT (appendix Q.1) and therefore does NOT scale with (d,n).  Window = [oracle-S>0.80 edge, ambient<0.30 edge].
  - source: scripts/run_suica_m4_l2_threshold_continuum.py:878-900 (W-1 fidelity row, the energy criterion and the (k_tau/DIM)^(1/4) factor); baseline edges from results/m4_l2_threshold_continuum/gates.json
- **S-4** — field ~ lambda * r^q - kappa * V_person
  - source: scripts/run_suica_m4_k2e_double_matching.py:3 (H-VAR statement) and :234-241 (person_share_design); r from scripts/run_suica_m4_k2c_matched_pairs.py:186-191 (predicted_attenuation -> k2b:533-583)
- **S-5** — the L3 spectral taxometer reads eta: eta_hat = eta within its CERTIFIED budget
  - source: scripts/run_suica_m4_l3_taxometer_meter.py:126 (X2_TOL) and results/m4_l3_taxometer_meter/decision.json leans/X-2 (10/10 within tolerance)

### G1D purity attestation (this run)

- world/panel/card generator calls: **0**
- guards installed and armed: **15**
- third-party (numpy/scipy) RNG calls, excluded by immediate-frame attribution: `46` (all from `scipy.stats`'s import-time docstring builder)
- SUICA-attributed RNG calls, whole run:

| phase | entry point | caller (file:line) | argument |
|---|---|---|---|
| `G0D-algebra` | `np.random.default_rng` | `suica_core/v8_realtext_relation_field.py:198` | `(20260822,)` |

**Purity note D1-PN-1 (disclosed).** The single SUICA-attributed RNG call the five entries reach is `suica_core/v8_realtext_relation_field.py:198`, `frozen_random_directions(seed = spec.seed + 17)`, reached through `k2b.layout()` while S-4 asks the K2a-validated attenuation algebra for `r`. It draws the FROZEN GAUGE DIRECTIONS — a frozen operator constant of the deployed gauge, fixed by a frozen spec seed. It is not a world, not a response panel, not a card, and it is independent of every knob this leg varies (`share`, `phi`): the same draw happens for every arm K2b/K2c/K2d/K2e ever ran. No author, occasion, response or card is generated anywhere in this leg — the raising stubs above prove it, and `k2c.predicted_attenuation`'s own docstring (k2c:186-191) certifies the route: *"Pure algebra: no world is generated."* The call is disclosed here rather than engineered around, because engineering around it would mean reimplementing published machinery and breaking rule 12.

### The hash

- **sha256 (salted)** = `3a1971b827210b7f3611b4769496f9d55d4ea815b6b8b577cae81f64b1fe00f8`
- construction: `sha256((salt + json.dumps(predictions, sort_keys=True)).encode()).hexdigest()`
- salt: 32 random bytes, `secrets.token_hex(32)`; stored ONLY in the bundle; never committed, never printed
- bundle: `results/d1_sealed/D1_SEALED_BUNDLE.json` (20205 bytes, gitignored via `.gitignore:2`)
- sealed at: `2026-08-09T17:41:45.071110+00:00`

### G2D — seal integrity, re-verified after writing

- bundle re-read from disk and re-hashed: `3a1971b827210b7f3611b4769496f9d55d4ea815b6b8b577cae81f64b1fe00f8`
- equals the committed hash: **True**
- bundle carries the full prediction table: **True** (5 entries)
- bundle carries the salt-handling statement: **True**
- bundle carries the opening protocol: **True**

### G3D — outcome (rule 16, trivial enumeration)

| outcome | condition | fired |
|---|---|---|
| `SEAL-COMPLETE` | all five entries computed, sealed, hash committed | **YES** |
| `SEAL-PARTIAL` | any entry uncomputable from published constants — named, sealed without it | no |
| `SEAL-FAIL` | protocol failure | no |

**Verdict: `SEAL-COMPLETE`.** All five registered entries (S-1..S-5) were computable from PUBLISHED constants and PUBLISHED formulas alone, with zero world generations. No entry was dropped, so SEAL-PARTIAL does not fire. The hash re-verified against the bundle after writing.

### Opening protocol (registered now, binding later)

1. The seal opens ONLY in a future registered leg (D-open).
2. Trigger: the program owner's request, or the defense phase reaching D4, whichever comes first.
3. (1) D-open RUNS the five configurations FRESH and writes its measured values to disk BEFORE any unsealing.
4. (2) D-open then adjudicates each measured value against the sealed band -- the bands are inside the bundle, so the adjudication rule is fixed before it is read.
5. (3) D-open unseals: it reads results/d1_sealed/D1_SEALED_BUNDLE.json, recomputes sha256((salt + json.dumps(bundle['predictions'], sort_keys=True)).encode()).hexdigest(), and requires it to equal the hash committed in reports/SUICA_D1_PROSPECTIVE_SEAL_REPORT.md.  A mismatch is SEAL-TAMPERED and the whole seal is void.
6. (4) D-open scores each entry PREDICTED / MISSED with NO re-fitting and NO post-hoc band edits.
7. (5) S-3 is CONJECTURE-GRADE: its miss kills the (d/n)^(1/4) window conjecture only.  S-1/S-2/S-4/S-5 misses fall on the measured laws.
8. If the bundle is lost, the seal is void and must be declared void -- a lost bundle is NOT evidence for or against any entry.

### Timing and anomalies

- Part 0 written: `2026-08-09T17:41:45.069315+00:00`
- seal produced: `2026-08-09T17:41:45.071110+00:00`
- wall time, whole run: `0.875` s (budget: arithmetic only, target < 10 min)
- import of the K/L-line reuse graph: `0.855` s; G0D provenance: `0.016` s; five entries: `0.001` s

Anomalies, with timing:

1. **`t≈0 s`, pre-run, protocol.** OP-31's `results/` artifact does not exist on this machine and `results/` is gitignored in full, so OP-31's seal of record is the tracked doc plus the tracked script constant. This does not block D1 — the fallback's storage model was pre-declared — but it means OP-31's own bundle is unverifiable here.
2. **`t≈0 s`, pre-run, provenance.** κ is not in the file the registration cites, and its persisted sign is negative where the registration writes it positive. Both corrections are in Part 0.2; neither changes a number.
3. **`t≈1 s`, purity harness, first draft.** A first purity harness poisoned `np.random.default_rng` unconditionally and crashed at IMPORT — inside `scipy.stats`, which calls `default_rng(615681484984984)` while building distribution docstrings. The harness was rewritten to ATTRIBUTE calls by stack frame instead of blanket-poisoning, so third-party RNG use is excluded and SUICA RNG use is logged rather than hidden. Corrected before any entry was computed.
4. **`t≈1 s`, purity harness, second draft.** The generator guard list initially included `k2a.splits`, which raised on a legitimate call: `splits` is a deterministic occasion-INDEX partitioner (k2a:335-340), not a generator. It was removed from the guard list before any entry was computed; `build_world`, `response_panel`, `card` and `centered_channels` stayed.
## Part 2 — leak check on this report (G2D sub-gate)

Every prediction-bearing numeric leaf of the sealed table is searched for, verbatim, in the text of this report. The two published L2 edges disclosed in Part 0.2 are whitelisted; everything else must be absent.

- prediction-bearing values checked: `49`
- whitelisted (published L2 edges): `2.6977587972014083`, `4.238555648405859`
- leaks found: **0**
- clean: **True**

The salt does not appear in this report; the bundle does not appear in git (`.gitignore:2`).
