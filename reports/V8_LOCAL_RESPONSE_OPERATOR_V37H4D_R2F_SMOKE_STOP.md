# V8 H4D-R2F Smoke Stop and R2F.1 Correction

Date: 2026-07-27

Original smoke decision:
`V8_R2F_STOP_INVALID_DATA_GENERATION`

Scientific disposition:
`SUPERSEDED_BEFORE_FORMAL_OUTCOMES`

The initial smoke produced two-parent engineering data but failed its finite
macro-coordinate gate. The new Jacobian-kernel chart was not the cause:

- maximum axis drift: `.00295 SD`;
- maximum mixed-corner drift: `.00139 SD`;
- maximum Jacobian-kernel residual: `2.71e-16`.

The sole failure was the legacy R2E.2 tangent-null arm. In one heavy-tail
parent, `operator_rho3` changed by only `1.51e-6`; because the frozen
baseline SD was `1.63e-5`, this became `.0927 SD`.

That legacy arm was never generated by the R2F Jacobian-kernel projection.
It is an external sanity control. Applying the new chart's finite-drift
identity gate to it was therefore a category error.

Before any formal R2F discovery run, R2F.1:

- kept `phi=.4`, all detector settings, dimensions, steps, thresholds,
  sample sizes, and bootstrap rules unchanged;
- limited the finite macro-drift gate to the new axis and corner geometries;
- retained the legacy tangent only under its independent outcome
  requirement \(UCB(J)\le.005\);
- used fresh smoke and formal root seeds.

No initial smoke detector outcome was inspected to make the correction. The
initial smoke is noncanonical and must not be used as scientific evidence.
See `docs/V8_LOCAL_RESPONSE_OPERATOR_V37H4D_R2F1_CORRECTION.md`.
