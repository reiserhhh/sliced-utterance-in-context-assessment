# V8 Resolution Filtration V3.7H Independent Mathematical Audit

Date: 2026-07-26

Ruling: `NO_GO_FOR_CONFIRMATION`

Engineering integrity passed and the stationary-core discovery signal is
accepted with a narrow boundary. The current protocol must not be frozen as
a confirmation protocol.

## Accepted discovery statement

For

\[
M_m=\widehat\mu_0+W_m(\bar X_m-\widehat\mu_0),
\qquad
\mathcal F_m=\sigma(X_1,\ldots,X_m),
\]

a strict martingale would require

\[
\mathbb E[M_{m'}\mid\mathcal F_m]=M_m.
\]

V3.7H instead tests a finite observable predictor family
\(\mathcal H\):

\[
\kappa_{\mathcal H}
=
1-
\frac{\|\Delta M-\widehat h(Z_m)\|^2}
{\|\Delta M\|^2}.
\]

The registered core worlds produced:

- simultaneous maximum \(\kappa\) upper bound 0.00296;
- mean-update energy upper bound 0.00547;
- monotone adjacent scorer-only NMSE improvement;
- 79.6%-90.0% endpoint NMSE reduction;
- minimum simultaneous model-assisted coverage lower bound 0.950.

This supports only a **function-class-bounded approximate martingale-like
resolution path** in four registered stationary synthetic worlds. It does
not establish a strict martingale, all-function coherence, real-text
existence, personality meaning, clinical validity, or arbitrary opportunity
robustness.

## Target aliasing

The opportunity world is

\[
X_m=\mu_0+G+f_m A G+\varepsilon_m,
\qquad
f_m=(m-32)_+/m.
\]

The scorer truth remains \(\mu_0+G\), while the observable proxy at 512
events contains approximately \(G+0.9375AG\). Therefore model selection and
error mapping are rewarded for approaching a contaminated target.

Observed:

- proxy NMSE fell from 0.716 to 0.098;
- true NMSE moved from 0.802 to 1.140;
- nominal coverage moved from 0.973 to zero;
- the radius narrowed from 2.401 to 0.799.

This is a confidently-wrong identification failure, not ordinary estimator
variance. Local \(\kappa\) cannot certify validity because it measures
predictability of updates, not whether updates move toward the intended
construct.

## Control limitation

The oscillating assay shows that the code path can detect one strong,
coordinate-aligned alternating perturbation. Its \(\kappa\) values were
0.110, 0.141, 0.335, and 0.491. It is not a matched power result for random
rotation, low rank, nonlinear response, or slowly accumulating opportunity
drift.

The discovery runner also copied the main-arm transition record when
exporting oscillating-assay rows and replaced only a subset of fields.
Oscillating \(\kappa\) is valid, but its inherited risk, proxy-loss, energy,
and decomposition fields must not be interpreted. The export must be fixed
before another run.

## Required V3.7H.1 repairs

1. Add a scorer-only projection-identity audit; keep it separate from the
   operational observable protocol.
2. Add cumulative path predictability and pathwise drift measures rather
   than relying only on adjacent updates.
3. Add paired panels with at least two randomized opportunity schedules.
   If their author scores disagree beyond a stationary calibration
   threshold, output `REFUSE/UNRESOLVED`.
4. Build a prospectively frozen power curve over low-rank, random-rotation,
   nonlinear, and multiple-strength violations.
5. Treat scorer-only NMSE increase and coverage collapse as synthetic stress
   failures; do not misuse a fixed \(\kappa>0.10\) rule as validity.
6. State explicitly that max-T bounds are one-sided and identify which side
   belongs to each gate.
7. Correct oscillating-assay row semantics.
8. Freeze code, formulae, gates, and a clean public timestamp before drawing
   a fresh confirmation seed.

The current 30-repetition output remains discovery evidence permanently.
